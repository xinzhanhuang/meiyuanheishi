# 工程化升级任务矩阵

> 目标：不改变现有页面效果和核心操作顺序，逐步建立可验证、可回退、可扩展到多院校的工程边界。

## 一、交付门禁

每个任务必须同时满足：

- 只解决一个明确问题，不夹带页面改版。
- 保留旧数据、旧分享链接和旧消息字段兼容。
- 有至少一个可运行的专项检查，并通过全量现有检查。
- 修改记录写入 MD 并打勾，独立 Git 提交后推送。
- 不提交 `project.config.json` 和无关的本地环境改动。

## 二、任务矩阵

| 优先级 | 工程域 | 当前状态 | 下一交付物 | 验收依据 |
| --- | --- | --- | --- | --- |
| P0 | 环境隔离 | 进行中 | AppID 到云环境唯一映射 | 新旧 AppID 单元检查，未知 AppID 拒绝启动云能力 |
| P0 | 数据权限 | 待控制台验证 | 13 个集合权限与索引清单 | 登录、发帖、评论、资料更新无 `-502003` |
| P0 | 云端写入 | 待契约化 | 点赞、评论、删除、投票、审核输入/输出契约 | 权限在云端判断，重复请求不重复写入 |
| P1 | 身份状态 | 最小 OpenID 账号已完成 | 登录错误码和用户状态归一 | 新用户、旧用户、封禁、网络失败结果明确 |
| P1 | 路由定位 | 新链路已统一 | 清理新链路对临时全局变量的剩余依赖 | 分享、消息、登录返回定位同一评论 |
| P1 | 实时监听 | 代码保护已完成 | 新环境断网/恢复与红点回归 | 单 watcher、有限重连、页面卸载后关闭 |
| P2 | 数据访问 | 部分列表/详情已优化 | 数据访问层的最小试点 | 页面不再重复写默认值、查询和错误映射 |
| P2 | 历史代码 | 待盘点 | `plate2` / `plate-zhoubian` 无引用实现清单 | 全局搜索、WXML 绑定、分享和生命周期证据 |
| P2 | 可观测性 | 未统一 | 关键云函数请求 ID 与错误分类 | 可按用户、帖子、学校和请求追踪失败链路 |
| P3 | 多院校 | 未启用 | `schools` 与默认 `schoolId` 兼容层 | 单校体验不变，旧数据默认归属天津美院 |

## 三、当前执行顺序

1. 完成环境隔离和新云环境基线验证。
2. 建立 21 个有效云函数的输入、输出、写入集合和权限契约。
3. 从评论链路开始试点统一数据访问与错误处理。
4. 盘点并分批清理详情页无引用历史代码。
5. 完成单校回归后引入 `schools/schoolId`。
