# 页面跳转与字段链路盘点

## 目的

记录当前小程序帖子、评论、分享和登录返回的实际参数链路，为后续统一字段做兼容改造。本文件只盘点，不改变现有页面行为。

## 现有详情页目标

| 目标页面 | 当前主集合 | 当前关键参数 | 已有能力 |
|---|---|---|---|
| `pages/plate2/plate2` | `ss` | `id`、`fenxiang`、`liuyan`、`commentId` | 普通帖、投票、评论定位和高亮 |
| `pages/plate-zhoubian/plate-zhoubian` | `tianmeizhoubian` / `tj` | `id`、`zhoubianfenxiang`、`liuyan`、`commentId` | 周边帖、留言、评论定位和高亮 |
| `pages/plate1/plate1` | `ss` | `choosetitle`、`choosetitle1`、`fenxiang` | 分类列表 |
| `pages/zuiretiezi/zuiretiezi` | `ss` | `fenxiang`、`choosetitle1`、`bannerList2` | 热帖列表 |

## 当前主要入口

| 来源 | 目标 | 现有参数特点 | 风险/后续处理 |
|---|---|---|---|
| 首页 `index` | 普通详情 | `id` 加点赞、作者、热帖、分类等冗余参数 | 新链路只保留帖子定位参数，旧参数继续兼容 |
| 板块 `plate1` | 普通详情 | `id`、`love`、`choosetitle1`、`zuiress_xx1` | 分类数组和热帖数组不应长期放在 URL |
| 搜索 `plate4` | 普通详情 | 同首页，携带分类/热帖大对象 | 后续统一为定位参数 |
| 热帖 `zuiretiezi` | 普通详情 | `id`、分享状态、分类/热帖参数 | 已修复分享状态赋值判断 |
| 消息 `message/xiaoxi` | 普通/周边详情 | `id`、`liuyan`、`ssid`、`commentId` | 当前已可定位并短暂高亮评论 |
| 个人主页 `checkuser` | 普通详情 | `id`、作者、点赞、订单等参数 | 详情页可自行读取的参数应逐步退出新链路 |
| 审核/管理页 | 普通/周边详情 | `id`、`fenxiang`、`liuyan` | 后续补 `source=admin` 与学校范围 |
| 分享卡片/朋友圈 | 普通/周边详情 | `id`、`fenxiang` 或 `zhoubianfenxiang`、部分横幅数据 | 必须兼容旧 `ture` 拼写 |

## 当前全局状态依赖

| 全局字段 | 当前用途 | 后续方向 |
|---|---|---|
| `app.fxssid` | 登录后返回帖子 ID | 新链路改为待恢复目标中的 `postId` |
| `app.fenxiang` | 普通帖分享/登录返回状态 | 新链路由 `source`、`postType` 替代，保留兼容 |
| `app.zhoubianfenxiang` | 周边帖分享/登录返回状态 | 同上 |
| `app.ssinfo` | 从详情返回列表时同步点赞、评论、浏览数 | 暂不替换，后续逐步收敛 |
| `app.zuiress_xx1` | 热帖缓存与跨页传递 | 仅作本地缓存，避免作为新 URL 参数 |
| `app.bannerList2` | 轮播图缓存 | 仅作本地缓存，避免作为新 URL 参数 |

## 统一字段映射（后续新增链路）

| 统一字段 | 兼容读取来源 | 说明 |
|---|---|---|
| `postId` | `postId` 或旧 `id` | 帖子主文档 ID |
| `postType` | `postType`；普通帖默认 `ss`；周边帖默认 `zhoubian`；留言为 `tj` | 决定详情页和集合 |
| `commentId` | 现有 `commentId` | 可定位主评论或回复 |
| `replyId` | 新字段，缺失时为空 | 回复级定位预留 |
| `source` | 新字段，缺失时按入口推断 | `index/message/share/search/profile/admin` |
| `schoolId` | 多院校阶段新增 | 所有跨校数据的隔离边界 |

## 登录后返回现状与目标

当前登录返回主要依赖 `app.fxssid`、`app.fenxiang`、`app.zhoubianfenxiang`，可返回帖子，但无法统一保证评论定位信息完整。

后续最小兼容目标：

```js
{
  postId: '...',
  postType: 'ss',
  commentId: '...',
  source: 'share',
  expiresAt: 0
}
```

登录或资料补全成功后：打开对应详情页 → 读取最新帖子 → 定位 `commentId` → 高亮 → 清除待恢复目标。

## 后续执行清单

- [x] 完成现有帖子、评论、分享、消息与登录返回入口盘点。
- [x] 建立兼容式 `postId/postType/commentId/replyId/source` 解析；详情页读取新 `postId`，缺失时继续读取旧 `id`。
- [x] 消息页跳转已同时携带统一参数和旧参数，旧链接与新链接都可定位评论。
- [x] 接入登录后待恢复目标：未登录分享详情保存 30 分钟有效的帖子/评论目标；登录或资料补全后优先恢复并清除该目标。
- [x] 普通帖、周边帖和留言帖的新分享链接只传定位字段及必要的派单身份字段，不再传轮播图、微信图片或热帖数据；旧分享链接继续解析。
- [ ] 多院校阶段为上述统一字段增加 `schoolId`。

## 不变保证

本盘点不修改任何代码、数据库、页面参数或线上数据；现有入口继续按原方式运行。
