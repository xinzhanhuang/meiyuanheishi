# 多院校升级与链路治理方案

## 1. 总原则

本方案在现有单校小程序稳定运行的基础上渐进实施，不重写现有项目，不直接修改线上数据结构，不以牺牲当前用户体验换取架构升级。

每个阶段都必须满足以下条件：

- 除明确的产品决策外，现有页面入口、TabBar、按钮、文案、样式、动画和操作顺序保持不变。
- 现有用户可继续登录、发帖、评论、回复、点赞、投票、分享、查看消息和使用周边功能。
- 旧帖子、旧消息、旧分享链接和旧数据库字段继续可用。
- 未登录用户从朋友圈、好友转发或消息入口进入时，登录完成后仍返回原帖子、原评论位置。
- 任一阶段失败可以按独立 Git 提交回退，不影响线上环境。
- 代码改动必须先在测试云环境和开发者工具验证，再决定是否上线。

### 1.1 当前产品决策（2026-08-29）

- 周边点评暂不作为多院校首期功能，底部“点评” Tab 已隐藏。
- `pages/tools`、`post-zhoubian`、`plate-zhoubian` 和 `tianmeizhoubian` 保留，不删除代码或数据，方便回退和后续决策。
- 旧分享和消息链接仍可打开已有周边详情；新用户不再从 TabBar 进入周边功能。
- 多院校第一阶段只覆盖 `users`、`ss`、`tj`、投票、搜索、消息、系统配置和管理权限。

## 2. 当前项目基础

当前小程序包含首页、板块、搜索、热帖、普通帖子、周边帖子、评论回复、消息、投票、审核和后台等功能。

当前主要业务集合包括：

- `users`
- `ss`
- `tianmeizhoubian`
- `tj`
- `VoteOption`、`VoteRecord`
- `searchLogs`
- `system`、`lunbotu3`
- `work_queue`

当前已具备消息跳转评论、高亮定位、用户实时监听和多项列表/详情页性能优化；仍存在页面参数不统一、整份对象跨页面传递、登录返回依赖全局变量和单校字段强绑定的问题。

## 3. 不可变更的体验基线

以下情况必须逐项回归，任一项退化即停止后续升级：

1. 已登录用户从首页、板块、搜索、热帖打开普通帖子和周边帖子。
2. 未登录用户从分享卡片、朋友圈或消息通知打开帖子。
3. 未登录用户登录、补充资料后自动回到原帖子。
4. 消息页点击评论、回复或点赞后正确打开对应帖子。
5. 帖子详情页自动滚动到对应评论或回复，并短暂高亮后恢复正常样式。
6. 评论被删除、帖子被删除、分享参数缺失或过期时显示提示，不白屏、不死循环。
7. 发布、评论、回复、点赞、收藏、投票、派单、举报、删除和审核结果与当前版本一致。
8. 旧天津美院数据、旧分享链接和旧消息记录继续能读取和打开。

## 4. 统一字段与跳转约定

后续新链路只传递定位字段，不传递整份帖子、热帖、分类或轮播图数据。

| 字段 | 含义 | 现阶段 | 多院校阶段 |
|---|---|---|---|
| `postId` | 帖子主文档 ID | 必填 | 必填 |
| `postType` | `ss` / `zhoubian` / `tj` | 必填 | 必填 |
| `commentId` | 主评论或回复 ID | 可选 | 可选 |
| `replyId` | 回复 ID | 可选 | 可选 |
| `source` | `index` / `message` / `share` / `search` / `profile` | 可选 | 建议必填 |
| `schoolId` | 学校标识 | 暂不启用 | 必填 |

以下历史参数暂时兼容，但不再作为新链路依赖：

- `zuiress_xx1`
- `choosetitle1`
- `bannerList2`
- `openid`、`lzid`、`takeorderid`
- `fenxiang`、`zhoubianfenxiang` 的多种拼写

详情页必须以 `postId + postType` 查询最新数据；评论定位只使用 `commentId/replyId`。

## 5. 登录后返回链路

未登录用户进入需要身份的帖子或评论入口时，保存短期有效的待恢复目标：

```js
{
  route: '/pages/plate2/plate2',
  postId: '...',
  postType: 'ss',
  commentId: '...',
  replyId: '',
  source: 'share',
  expiresAt: 0
}
```

流程固定为：

```text
外部入口 → 解析定位参数 → 保存待恢复目标 → 登录/补资料
→ 登录成功 → 读取待恢复目标 → 打开原帖子 → 定位并高亮评论 → 清除待恢复目标
```

旧的全局变量跳转方式保留兼容，待新链路完成并验证后再逐步清理。

## 6. 执行阶段

### 阶段 A：稳定性检查与修复

- [ ] 完成当前云环境数据库权限验证：新用户登录、发帖、评论、资料修改不再出现 `-502003`。
- [ ] 在开发者工具和真机验证实时监听网络恢复、消息红点和页面切换。
- [x] 全局检查分享参数赋值式判断、缺失参数和重复加载，逐项独立修复。
- [x] 为关键跳转补充旧参数兼容与错误提示。

验收：单校现网完整回归通过，所有改动可独立回退。

### 阶段 B：字段与跳转链路盘点

- [x] 输出所有页面入口、出参、入参、依赖全局变量和数据库字段清单。
- [x] 输出消息类型与 `postId/commentId/replyId` 对应关系清单。
- [x] 标记哪些页面仍传递整份对象，哪些字段可以由详情查询替代。
- [x] 不改业务字段，只形成兼容映射和迁移顺序。

验收：任何一条“分享/消息/登录后返回”链路都可追溯其参数来源和目标页面。

### 阶段 C：统一帖子与评论定位

- [x] 新增统一的帖子定位参数解析，兼容旧 `id`、`fenxiang` 和历史拼写。
- [x] 新消息写入统一使用 `postId`、`postType`、`commentId`和 `source`。
- [ ] 回复级消息写入补齐 `replyId`，并保留旧 `commentId/id` 兼容。
- [x] 普通帖、周边帖和留言帖复用同一定位结果，保持各自页面 UI 不变。
- [x] 新分享链接只传定位字段；旧分享链接继续解析。

验收：同一评论从消息、分享、个人主页等不同入口都能定位到同一位置。

### 阶段 D：统一登录后返回

- [x] 用待恢复目标替代新链路中的临时全局跳转变量。
- [x] 登录、资料补全、取消授权、目标帖子删除等分支都有明确结果。
- [x] 保留旧 `app.fxssid` 等逻辑作为兼容兜底，验证后再清理。

验收：未登录用户经任意入口登录后，不丢失原帖子与评论定位。

### 阶段 E：性能与历史代码整理

- [x] 列表页数据访问边界已收口：`plate1`、`plate4`、`zuiretiezi` 共用最小 `post-list-service` 查询入口，页面过滤条件、排序、分页、请求时机保持不变；重复请求与评论遍历的进一步削减仍单独评估。
- [x] 普通发帖职责收口：`pages/post/post` 保留表单校验、提示和页面状态；帖子基础对象组装、文字/图片审核、图片上传与发布调用统一进入现有 `post-service`，审核复用 `comment-service`，普通帖/派单帖字段与调用顺序保持不变。已通过静态边界、对象组装和上传路径行为检查，开发者工具发帖回归待进行。
- [x] 已完成 `plate2.js` 未使用历史实现盘点与清理：删除前经 WXML、同页代码和已拆 `plate2-*` 模块交叉搜索确认无引用；`plate-zhoubian.js` 留待后续单独处理。
- [x] 首页和个人中心已完成引用级轻量化：清理孤立状态、无引用方法、重复红点处理与无效日志，保留现有加载、登录、监听、跳转和发布体验。
- [x] 盘点云函数目录、云端触发器和外部调用，确认后再清理空目录。
- [ ] 为高频查询建立索引方案，不变更字段语义。

验收：核心流程无行为变化，页面打开和分页加载不回退。

### 阶段 F：多院校测试环境与数据模型

- [ ] 新建测试云环境，导出并复制必要集合、数据和已使用云函数。
- [x] 建立本地默认学校契约与 `currentSchoolId`运行时上下文；当前固定 `tjarts` 对应天津美术学院，界面仍为单校。
- [ ] 新建 `schools` 学校配置集合，并将本地契约改为云端配置的安全回退。
- [ ] 为 `users`、帖子、评论所属主文档、投票、搜索记录、工单等增加 `schoolId`。
- [ ] 将现有天津美院数据统一迁移为固定 `schoolId`。
- [ ] 所有查询、消息、分享和管理员权限默认按 `schoolId` 隔离。
- [ ] 周边点评是按学校、城市共享还是下线，待产品决策后单独设计，不阻塞首期多院校。

验收：测试环境中 A 校用户不能读取、评论或管理 B 校数据；旧天津美院数据和链接保持兼容。

### 阶段 G：灰度上线

- [ ] 先仅开放天津美院，使用新字段但保持单校体验。
- [ ] 白名单接入第二所学校，完成真实用户回归。
- [ ] 校验跨校隔离、管理员范围、分享链接和审核数据。
- [ ] 通过后逐步扩大范围，不直接全量切换。

验收：每所学校可独立停用、回滚和排查，线上用户无感完成升级。

## 7. 数据库权限与安全边界

当前项目仍有前端直接写数据库的旧链路。多院校正式上线前，以下操作应逐步迁移至云函数或安全规则保护：

- 点赞、评论、回复、投票等跨用户修改帖子文档的操作。
- 删除、审核、封禁、派单、工单管理等管理员操作。
- 跨学校读取、写入、统计和搜索。

目标不是一次性重写，而是先保留旧功能，再将高风险写操作逐项迁移，并保证返回值和用户提示兼容。

## 8. Git 与交付规则

- 每个阶段再拆成可独立验证的小任务。
- 每个任务只提交相关文件，禁止提交 `project.config.json`。
- 提交前执行语法检查、已有静态检查、`git diff --check` 和流程回归。
- 通过后自动推送至 `codex/project-optimization`，不直接合并主分支。
- 每完成一项，在本文件及 `项目优化修改标准.md` 中勾选并记录验证结果。

## 9. 当前推荐顺序

1. 完成阶段 A 剩余的数据库权限和真机回归。
2. 执行阶段 E：建立云函数契约、索引方案，分批清理可证明的历史实现。
3. 完成测试环境的集合、权限、索引、云函数和云存储迁移。
4. 进入阶段 F：新增 `schools/schoolId`，先保持单校界面与旧数据兼容。
5. 阶段 G 只在跨校隔离和核心回归全部通过后开始。

本文件的底线：**新架构只能在不改变现有用户体验、兼容旧数据和旧链接的前提下逐步替换旧链路。**
