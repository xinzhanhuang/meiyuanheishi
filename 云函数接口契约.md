# 云函数接口契约

> 本文记录当前代码的真实接口，不代表所有接口已经安全。修改云函数参数、集合写入或返回结构前，必须同步本文并运行 `node scripts/check-cloud-function-contracts.js`。空目录不计入云函数。

## 一、统一约定

- 调用来源分为：`前端`、`定时器`、`后台/备用`。
- `当前身份边界` 描述现状：仅 `login`、微信安全接口可从可信上下文取得身份；其他写函数多数仍信任前端参数，后续必须逐个收口，不能一次性改断旧客户端。
- `当前返回` 保留现有形态。本轮不统一返回结构，避免影响现有页面判断。
- `event.userInfo.openId` 是旧版云函数注入字段；后续改造应迁移到 `cloud.getWXContext().OPENID`，迁移前需保留兼容。

## 二、接口总表

| 云函数 | 来源/触发 | 主要输入 | 读取集合 | 写入集合 | 当前返回 | 当前身份边界 |
| --- | --- | --- | --- | --- | --- | --- |
| `AiNews` | 定时器；每天 16:00 | 无 | 外部校园网页/AI 接口 | `ssss` | `{success, count/results/error}` | 后台任务，无用户身份 |
| `VoteOption` | 前端投票/投票状态读取 | 读取：`action=getVoteState`, `itemid`；v2 投票：`actionVersion=2`, `id`, `itemid`, `voteNumber`, `colorIndex`；旧版：`id`, `itemid` | `ss`, `VoteOption`, `VoteRecord` | `VoteRecord`, `VoteOption`, `ss` | 读取：`{success, options, record}`；投票：`{success, remaining/errCode}`；旧版返回 `legacy` 标记 | 读取和 v2 投票使用可信 OpenID；投票使用事务与票数上限；旧协议仅为在线旧客户端兼容 |
| `aggregateSearchKeywords` | 后台/备用 | 无 | `searchLogs`, `search_keywords` | `search_keywords` | `{success, msg/count/error}` | 后台聚合，无用户身份 |
| `checkImg` | 前端内容审核 | `media` | 无 | 无 | 微信图片审核结果或错误对象 | 调用微信安全接口，不写业务数据 |
| `checkStr` | 前端内容审核 | `text` | 无 | 无 | 微信文本审核结果或错误对象 | 调用微信安全接口，不写业务数据 |
| `checknotice` | 管理员审核通知 | `wbnr`, `username`, `id` | `users`, `system` | 无业务集合 | `{success, sent, failed}` 或权限/参数错误 | 仅云端管理员可调用；接收者从 `system01.glids_openid` 读取，不接受前端名单 |
| `delete` | 前端删除评论/回复 | `_data.{liuyan,type111,id0,id1,time,time1,id}` | `users`, `system`, `ss` / `tianmeizhoubian` / `tj` | 对应帖子集合 | `{success, deleted, collection, removedCount}` 或明确错误码 | 使用可信 OpenID；仅评论作者、帖子作者或 `system01.system.glids` 管理员可删除 |
| `dianzan` | 前端点赞/码住 | `id`, `type`, `plid`, `collection` 及通知展示字段；码住使用 `type=mazhu` | `ss`, `tianmeizhoubian`, `users` | `ss`, `tianmeizhoubian`, `users` | 点赞：`{success, liked, type}`；码住：`{success, bookmarked}`；或明确错误码 | 使用可信 OpenID 反查用户文档 ID；类型和评论集合使用白名单；码住使用事务切换当前用户状态 |
| `fbpl` | 前端普通帖评论/回复 | `pinglunnr`, `pd`, `Mazhu` | `ss` / `tj`, `users` | `ss` / `tj`, `users` | 参数/身份错误或原数据库结果 | 评论人 ID、评论时间和评论 ID 由云端可信身份生成；回复目标仍兼容旧字段 |
| `fbzbpj` | 前端周边帖评论/回复 | `pinglunnr`, `pd` | `tianmeizhoubian` / `tj`, `users` | `tianmeizhoubian` / `tj`, `users` | 参数/身份错误或原数据库结果 | 评论人 ID、评论时间和评论 ID 由云端可信身份生成；回复目标仍兼容旧字段 |
| `getSearchSuggestions` | 前端搜索联想 | `keyword` | `searchLogs` | 无 | `{list}`；失败也返回空列表 | 只读，无用户身份 |
| `getTagPostCount` | 前端标签统计 | `tags` | `ss` | 无 | 标签计数对象；失败返回 `{}` | 只读，无用户身份 |
| `getnewlines` | 定时器；每天 23:30 | 无 | 外部文章接口, `work_queue`, `fetch_states` | `work_queue`, `fetch_states` | `{success, stats/msg/error}` | 后台任务，无用户身份 |
| `getphone` | 前端手机号解密 | `id`（CloudID） | 微信开放数据 | 无 | `cloud.getOpenData` 结果 | CloudID 由微信开放数据接口校验 |
| `getworkmessage` | 定时器每天 12:00；后台页手动调用 | `url` 或 `articleId`；无参处理队列 | `work_queue`, `ss`, `users`, `system`，外部文章/AI 接口 | `work_queue`, `ss` | `{success, msg/stats/task/content/error}` | 定时器无用户身份；带 URL/文章 ID 的手动调用要求云端管理员身份 |
| `jubao` | 前端举报普通帖 | `id`, `time`, `ywnr` | `ss`, `users` | `ss`, `users` | 原结果或 `ALREADY_REPORTED/REPORT_LIMIT_REACHED` | 使用可信 OpenID 反查举报人，同一用户不能重复举报 |
| `jubaoplus` | 前端管理员封帖/举报 | `id`, `time`, `ywnr`, `type` | `system`, `users`, `ss` / `tianmeizhoubian` | 对应帖子集合, `users` | 权限/参数错误或原异步结果 | 仅 `system01.system.glids` 中的管理员可调用，举报人 ID 由云端覆盖 |
| `login` | 前端登录/在线状态 | 无 | 微信调用上下文 | 无 | `{event, openid, appid, unionid}` | 使用 `cloud.getWXContext()`，身份可信 |
| `look` | 前端详情浏览计数 | `id`, `type`, `num` | 无显式读取 | `ss` / `tianmeizhoubian` / `tj` | `{success, type, increment, stats}` 或参数错误 | 无需用户身份；帖子 ID 必填，类型限制为三个已知集合 |
| `ordernotice` | 前端派单通知 | `orderid`, `takeorderphone`, `liuyan` 及兼容展示字段 | `ss` / `tj`, `users` | `users` | `{success, ownerId, subscriptionSent}` 或明确错误码 | 接单人来自可信 OpenID，接收者从帖子作者读取；拒绝自己接自己的派单 |
| `publishPost` | 前端发布普通帖/派单帖 | `ss_xx`, `voteNumberPerPerson`, `voteOption` | `users` | `ss`, `VoteOption`, `users` | `{success, id, record}` 或身份/参数错误 | 使用可信 OpenID 反查作者；作者 ID、时间和个人资料展示字段由云端覆盖；三类写入处于同一事务中 |
| `update_post_status` | 前端审核页 | `id`, `status`, `reason` | `system`, `users`, `tianmeizhoubian` | `tianmeizhoubian`, `users` | `{stats, notificationSent}` 或 `{errCode, errMsg}` | 仅云端管理员可调用，状态限制为通过 `1` 或拒绝 `2` |

## 三、必须保持的兼容边界

1. 消息对象继续同时保留 `postId/postType/commentId/source` 与旧 `ssid/type/subtype/liuyan`。
2. 评论写入继续兼容 `ss`、`tianmeizhoubian` 和留言集合 `tj`。
3. 现有页面依赖 `checkImg/checkStr` 的微信原始 `errCode`，不得直接包成新结构。
4. `login.result.openid` 是全局登录、首页、详情和后台页的共同依赖。
5. `getSearchSuggestions` 失败时必须继续可降级为空列表，不能阻塞本地搜索历史。
6. 定时任务的触发时间和队列集合在迁移环境时必须单独核对。

## 四、后续收口顺序

- [x] 第一组：`VoteOption`、`dianzan`、`look`，已补可信身份或无身份边界、参数白名单、事务写入和稳定返回；重复调用验证纳入最终统一检测。
- [x] 第二组：`fbpl`、`fbzbpj`、`delete`，已完成可信评论人入口、删除归属校验、集合白名单和明确删除结果；完整异常映射纳入最终统一处理。
- [x] 第三组：`jubao` 使用可信举报人；`jubaoplus`、`update_post_status`、`getworkmessage` 手动入口已补管理员身份校验。
- [x] 第四组：`ordernotice`、`checknotice` 已限制可信发送者、帖子接收者、管理员权限和通知名单来源。
- [ ] 最后统一成功/失败协议；在所有旧页面适配完成前，不删除旧返回字段。

## 五、本轮验证记录

- [x] 22 个含 `index.js` 的有效云函数均在总表中且只出现一次。
- [x] 文档中的云函数名称与源码目录双向一致。
- [x] 三个本地定时触发器已记录：`AiNews`、`getnewlines`、`getworkmessage`。
- [x] 本轮仅新增契约和检查脚本，不修改云函数运行逻辑。
