# 天美黑市小程序优化修改标准

## 1. 总目标

在保证用户可感知的功能、交互、视觉效果和业务数据不变的前提下，逐步降低项目的重复代码、文件体积、数据库请求量和维护成本。

优化不以“改得更新”为目标，只处理已经确认的问题。

## 2. 不可变更的基线

除非任务明确授权，以下内容不得变更：

- 页面路由、TabBar 顺序和页面跳转结果。
- 按钮位置、文案、颜色、尺寸、动画和交互触发方式。
- 登录、发帖、评论、回复、点赞、收藏、投票、派单、举报和消息通知的业务规则。
- 云数据库集合名、字段名、字段类型和已有数据的含义。
- 云函数名、入参、返回值及错误语义。
- 云存储路径和已经存储的文件。
- 管理员、封禁、内容审核和订阅消息等安全规则。

## 3. 允许的优化

- 删除经证明无引用、无副作用的代码、文件和资源。
- 合并完全相同且业务语义一致的逻辑。
- 拆分超大函数或页面，但不改变调用顺序和外部接口。
- 减少重复数据库查询、重复 `setData` 和不必要的全字段传输。
- 补齐异常处理、加载状态和监听器释放，前提是不改变正常流程。
- 修复可以用明确证据证明的缺陷。
- 将硬编码的重复常量收敛到已有配置中，不为单一值新建抽象层。

## 4. 禁止事项

- 不得一次性重写整个页面或整个业务模块。
- 不得在同一个改动中同时做功能修复、UI 调整和全文件格式化。
- 不得为未来可能的需求增加框架、工具类、适配层或新依赖。
- 不得更换技术栈或引入新 UI 库、状态管理库、请求库。
- 不得修改与当前任务无关的文件。
- 不得覆盖、回退或删除现有未提交改动。
- 不得使用“看起来没用”作为删除依据。
- 不得在没有兼容方案和数据备份时修改数据库结构。
- 不得为了减少代码而削弱内容审核、权限判断、输入校验或错误处理。

## 5. 单次任务规模

每次任务只处理一个明确目标，例如“删除空云函数目录”或“合并某页面的重复加载逻辑”。

默认限制：

- 优先修改 1–3 个文件。
- 超过 5 个业务文件时，先停止并说明拆分方案。
- 优先使用现有组件、工具和平台 API。
- 改动只覆盖完成当前目标所需的最小范围。

## 6. 执行流程

### 6.1 改动前

1. 读取 `git status --short`，记录已有修改，不将其当作本次成果。
2. 阅读目标文件的完整逻辑，不只阅读待修改的几行。
3. 使用全局搜索查清调用者、事件绑定、云函数调用、数据库字段和资源引用。
4. 记录优化前的行为基线：入口、操作步骤、界面结果、数据写入和异常表现。
5. 如果无法证明改动安全，不修改，只报告风险和需要的验证条件。

### 6.2 改动中

1. 优先在真正的共享源头处解决问题，避免在多个调用点重复打补丁。
2. 保留旧的对外函数名、事件名、参数结构和返回结构。
3. 保留原有异步顺序，除非已证明调整后不会引入竞态。
4. 对数据库写操作、计数器、点赞状态和消息数组保留原子更新语义。
5. 不对整个文件执行格式化，只保持新改区域可读。
6. 发现额外问题时记录，不顺手扩大本次范围。

### 6.3 改动后

1. 执行语法、配置和可用的项目检查。
2. 逐项重跑本次涉及的用户流程。
3. 对比改动前后的页面、数据库写入、云函数返回和异常表现。
4. 检查 `git diff --check` 和本次 diff，确认没有无关改动。
5. 报告实际验证结果；未验证的项目必须明确列出，不得表述为“已通过”。

### 6.4 自动 Git 提交与推送

每个通过验收的独立步骤，Codex 可以自动创建提交并推送，无需每次重复询问。执行时必须遵守：

1. 首次执行前确认 Git 远程仓库、推送目标和分支。默认在 `codex/project-optimization` 分支工作，不直接向 `master` 推送优化提交。
2. 创建分支前先记录当前分支、`HEAD` 和工作区状态，不为创建分支而回退或藏起用户改动。
3. 每一步只暂存本步由 Codex 新增或修改的文件/行，禁止使用 `git add .`、`git add -A` 或其他全量暂存命令。
4. 如果目标文件在本步开始前已有未提交改动，必须确认能够只暂存 Codex 的新增行；无法安全分离时停止提交，不得把用户改动一起提交。
5. 提交前运行 `git diff --cached --check` 并审阅 `git diff --cached`，确认暂存区只有本步内容。
6. 只有在本步必要检查通过后才提交。验证失败、结果不确定或存在未解决冲突时不提交、不推送。
7. 一个提交只对应一个可回退的优化步骤。提交信息使用 `type(scope): 中文摘要`，例如 `refactor(cloudfunctions): 清理已确认的空目录`。
8. 提交成功后自动推送当前优化分支。禁止强制推送，禁止自动合并到 `master`，禁止自动删除远程分支。
9. 推送失败时保留本地提交，报告原因并停止该步骤的远程交付；不通过变基、强推或覆盖远程历史自行解决。
10. 每步报告分支名、提交短 SHA、提交信息和推送结果。

自动 Git 的前提是“可精确隔离本步改动”。它不授权 Codex 提交既有工作区内容，也不授权 Codex 改写 Git 历史。

## 7. 验收标准

单次优化只有同时满足以下条件才算完成：

- 原有用户流程可正常完成。
- 正常、空数据、未登录、加载失败等关键分支没有退化。
- 界面文案、布局、状态和跳转结果与基线一致。
- 数据库集合、字段和写入结果与基线兼容。
- 云函数的调用方式和返回结果兼容。
- 没有新增未关闭的 watcher、计时器、重复请求或重复提交。
- 没有引入新依赖，除非任务明确批准。
- diff 只包含当前任务所需改动。
- 有可复现的验证记录。

## 8. 云函数目录清理标准

### 8.1 分类

每个云函数目录必须先归入以下一类：

- **使用中**：前端、其他云函数、触发器或运维流程仍在使用。
- **可删除**：无有效源码，且无任何调用、部署、触发器或运维依赖。
- **疑似废弃**：本地未找到引用，但可能由云端触发器、控制台或外部系统调用。
- **平台/第三方函数**：CMS、微信支付或扩展能力相关，不参与普通空目录清理。

### 8.2 允许删除的必要条件

以下条件必须全部满足：

1. 目录内没有有效 `index.js` 或其他可执行业务源码。
2. 全项目未搜索到该云函数名的调用或配置引用。
3. 未被其他云函数 `require`、调用或共享文件。
4. 确认没有定时触发器、数据库触发器、HTTP 访问或控制台手工调用。
5. 不属于 CMS、微信支付、内容审核或其他平台生成功能。
6. 先输出拟删除清单和判定依据，获得确认后再删除。

仅本地搜索不到引用，不足以删除云函数。

### 8.3 清理交付物

- 云函数目录全量清单。
- 每个目录的分类、本地引用证据和处理建议。
- 拟删除清单和暂不删除清单。
- 删除后的全局搜索、配置检查和小程序编译结果。

## 9. 优化优先级

### P0：数据和安全

- 可能导致数据错写、重复写、计数错误或权限绕过的问题。
- 点赞、评论、删除、投票和派单等并发写操作。
- 内容审核、管理员权限和用户封禁。

### P1：明确缺陷和资源泄漏

- 条件判断写成赋值等确定性错误。
- watcher、计时器或事件监听重复创建且未关闭。
- 异常分支卡住加载、重复提交或返回错误状态。

### P2：性能

- 重复查询、N+1 查询、无分页列表、无必要全字段拉取。
- 大对象或整个列表的高频 `setData`。
- 重复图片处理、重复请求和无限制并发。

### P3：可维护性

- 删除已证实无用的代码和资源。
- 合并重复逻辑。
- 拆分超大页面和过长函数。
- 改善命名和必要注释，不进行为格式而格式的改动。

## 10. Codex 单次任务输出格式

每次执行完成后，必须简要输出：

1. **目标**：本次只解决什么问题。
2. **改动**：修改了哪些文件和逻辑。
3. **不变保证**：哪些对外行为保持不变。
4. **验证**：实际执行了哪些检查，结果如何。
5. **未验证项**：需要开发者工具、云环境或真机才能验证的内容。
6. **风险**：仍然存在的具体风险，没有则明确写“未发现”。
7. **Git 交付**：分支名、提交 SHA、提交信息和推送结果；未提交时说明原因。

## 11. 建议执行顺序

1. 建立可复现的核心功能回归清单。
2. 盘点云函数目录，只生成分类和拟删除清单。
3. 确认清单后删除空目录，执行编译和核心流程回归。
4. 修复 P0/P1 级确定性问题，每个问题单独处理。
5. 优化首页和列表查询、分页及 `setData`。
6. 处理普通帖与周边帖的可证明重复逻辑。
7. 最后拆分 `plate2.js`、`plate-zhoubian.js` 等超大文件。

---

本标准的核心原则：**先证明问题，再做最小改动；先验证兼容，再继续下一项。**

## 12. 当前稳定性专项清单

- [ ] **P0 数据库权限**：在当前云环境为旧版前端直写路径配置集合权限；完成标准为新用户登录、发帖、评论、资料更新均不再出现 `-502003`。需在云开发控制台完成，代码不修改数据库规则。
- [x] **P1 实时监听**：同一用户在任意时刻只保留一个 `users/{用户ID}` 实时监听；已通过模拟监听创建、消息更新、红点更新与关闭检查。真机/开发者工具切换页面验证待进行。
- [x] **P1 实时监听重连节流**：同一用户的重试已排队时，页面再次进入不再取消定时器并立即重连；保留最多三次自动重试和不同用户切换逻辑。已通过单监听、消息红点与重试排队检查，网络恢复后的开发者工具/真机验证待进行。
- [x] **P1 加载状态**：已为新用户授权创建/读取、自动登录查询、首页静默登录和首页列表请求补齐失败处理；首页移除了未对应 `showLoading` 的 `hideLoading`。已通过语法和静态保护检查，开发者工具实际操作验证待进行。
- [x] **P2 首页列表增量处理**：加载更多时只排序本次新增帖子中的评论；首屏与下拉刷新仍处理完整结果，排序规则和页面数据保持不变。已通过语法、增量范围和结果一致性检查，开发者工具分页验证待进行。
- [x] **P2 详情页重复加载**：普通入口不再在 `onLoad` 中重复读取同一帖子及其投票数据；分享入口保留原有提前加载时机。已通过语法、单次加载、分享参数与评论跳转检查，开发者工具详情页验证待进行。
- [x] **P2 详情页按需投票加载**：仅非空 `voteOption` 的投票帖查询 `VoteOption` 和 `VoteRecord`；普通帖跳过两次无效请求，空投票选项不再访问不存在的首项。已通过语法及投票条件、详情页和评论跳转检查，开发者工具投票帖与普通帖验证待进行。
- [x] **P2 详情页评论状态匹配**：评论刷新时按 `pinglunID` 一次建立旧状态索引，不再为每条新评论重复扫描旧列表；评论排序、图片已加载状态、历史异常 ID 和高亮定位保持原有行为。已通过语法、索引语义、投票、详情页与评论跳转检查，开发者工具多评论帖验证待进行。
- [x] **P2 周边详情页加载流程**：修正分享入口判断并让普通入口、分享入口各加载一次；横幅参数仅由分享入口读取，留言集合在加载前已设置。已通过语法、单次加载流程与评论跳转检查，开发者工具普通周边帖、分享周边帖与留言入口验证待进行。
- [x] **P2 板块、搜索与热帖列表增量处理**：`plate1`、`plate4`、`zuiretiezi` 加载更多时只排序新增帖评论；移除两个列表页中不改变数据的空遍历与空 `push.apply`。已通过三页语法、增量范围、结果一致性和评论跳转检查，开发者工具三页分页验证待进行。
- [x] **P2 首页用户重复读取**：首页 `onShow` 不再额外读取当前用户文档；消息、点赞消息和 Tab 红点继续由全局用户实时监听同步。已通过首页语法、重复读取、监听与加载状态检查，开发者工具消息红点实时验证待进行。
- [x] **P1 板块与热帖分享入口判断**：`plate1`、`zuiretiezi` 不再将分享参数或页面状态赋值为真；兼容 `true/ture` 分享参数，普通入口不再误触发分享登录或分享参数路径，分享横幅仅在真实分享入口读取。已通过两页语法、分享判断与列表增量处理检查，开发者工具普通入口和分享入口验证待进行。
- [x] **P3 页面跳转与字段链路盘点**：已记录帖子、评论、分享、消息、登录返回入口，以及现有 URL 参数和全局状态依赖；定义后续新增链路统一使用的 `postId/postType/commentId/replyId/source/schoolId` 映射。仅新增文档，不改变运行逻辑。
- [x] **P1 帖子跳转参数兼容**：普通详情和周边详情支持从新 `postId` 或旧 `id` 读取目标；消息页在保留 `id/liuyan/commentId` 的同时补充 `postId/postType/source=message`，评论定位与短暂高亮逻辑不变。
- [x] **P1 登录后恢复帖子评论**：未登录的分享详情会保存 30 分钟有效的帖子、类型和评论目标；登录或资料补全成功后优先回到详情并触发已有评论定位/短暂高亮，再清除目标。旧版 `fxssid/fenxiang` 返回路径继续保留。
- [x] **P3 详情页无效列表操作清理**：移除普通帖和周边帖详情页中的空 `forEach` 及未传入元素的 `push.apply`；评论排序、图片状态处理和 `setData` 保持不变。
- [x] **P3 项目结构、集合字段与云函数台账**：按有效源码口径统计 21 个云函数；99 个本地空目录不计入云函数数量，仅作为云端部署风险提示。已记录 20 个注册页面、代码可见集合/字段、跳转字段和云函数分类；静态证据不足以删除任何云函数，拟删除数为 0。
- [x] **P1 详情页空白与加载兜底**：普通帖和周边帖详情在缺少帖子 ID、目标不存在或数据库读取失败时进入原有“动态消失”状态；读取失败额外提示稍后重试，普通详情的加载动画在所有结果分支结束。已通过语法和异常分支静态检查，开发者工具验证待统一进行。
- [x] **P1 帖子分享参数瘦身**：普通帖、周边帖和留言帖的新好友/朋友圈分享统一携带 `id/postId/postType/source` 及必要兼容字段；普通帖不再把轮播图、微信图片或热帖数据放入分享 URL，旧分享参数继续兼容读取。
- [x] **P1 登录返回失败分支**：取消头像昵称授权会结束加载并明确提示；静默登录失败会恢复未登录状态；资料校验完成后才显示提交加载，数据库保存成功后才消费待恢复目标并跳转，保存失败保留目标供重试。目标帖子不存在由详情页失效状态承接。
- [x] **P1 历史帖子字段保护**：普通帖和周边帖读取后统一补齐 `ss_xx`、图片、评论、回复、点赞、举报、附件、派单对象及常用计数字段的安全默认值；只补缺失/类型异常字段，不覆盖现有有效数据。
- [x] **P1 在线状态失败处理**：前后台切换统一调用安全的在线状态更新方法；登录结果缺少 OpenID、云函数失败或数据库更新失败均被捕获，不产生未处理 Promise，版本更新检查不再依赖登录请求成功。
- [x] **P1 实时监听最终降级**：用户消息监听初次失败后最多自动重试三次；仍失败则停止排队并标记降级，保留当前用户数据和普通页面查询，下次进入前台重新尝试，避免持续报错或无限重连。
- [x] **P1 消息跳转类型统一**：回复、评论、点赞、评论点赞、派单、审核和举报的新消息写入 `postId/postType/commentId/source`；消息页优先使用新字段，旧 `ssid/type/subtype/liuyan` 继续兼容。普通详情与周边详情的留言均通过 `liuyan=true` 读取 `tj`，不再只凭集合名猜测页面。相关云函数源码已通过语法检查，部署待统一进行：`fbpl`、`fbzbpj`、`dianzan`、`ordernotice`、`update_post_status`、`jubao`、`jubaoplus`。
- [x] **P2 分享入口登录查询复用**：普通详情和周边详情从分享进入时优先复用全局已登录用户，仅在冷启动用户状态为空时调用 `login` 并读取 `users`；帖子仍按原时机展示，冷启动未登录目标继续保存用于登录后返回。
- [x] **P2 内部跳转参数瘦身**：首页、分类、搜索、热帖和普通详情返回分类页时，不再把分类数组或热帖数组序列化进 URL；改用已有全局热帖缓存和新增分类缓存，详情页继续兼容旧 URL 参数。
- [x] **P2 详情图片状态复用**：普通详情和周边详情共用纯函数生成主图、评论图和回复图加载状态；普通详情继续按 URL 匹配保留状态，周边评论继续按原索引规则保留，不改变占位图、懒加载和预览行为。
- [x] **P2 浏览量单入口计数**：首页、分类、搜索、热帖、个人主页、点评列表和详情内推荐跳转不再提前调用 `look`；普通详情与周边详情在 `onLoad` 统一计数一次，并按 `ss/tianmeizhoubian/tj` 选择正确集合。
- [x] **P2 搜索建议请求控制**：保留输入后 300ms 云端联想；每次输入使用递增请求标识丢弃过期响应，清空、提交、页面隐藏或卸载时同步取消待执行定时器并使在途响应失效，不改变本地历史、热词和云端结果的合并顺序。
- [x] **P1 无隐私资料登录**：登录只使用云函数 OpenID，不再调用 `wx.getUserProfile`；新用户以本地默认头像和“校园用户”建立最小账号，昵称、头像、性别、专业和手机号均为可选资料，不再强制跳转资料页即可使用普通发帖。涉及派单的联系方式仍按原业务规则必填。
- [x] **P1 详情互动资料可选**：`plate2` 评论、回复及其他详情互动只检查 OpenID 账号登录，不再强制手机号、昵称或性别；资料缺失时评论使用“校园用户”、本地默认头像和空性别/专业，账号封禁、文字与图片审核保持不变。
- [x] **P0 云环境按 AppID 隔离**：云开发初始化不再依赖手工改环境 ID；旧 AppID 固定连接原环境，新测试 AppID 固定连接新环境，未配置 AppID 明确拒绝连接，防止测试误写正式数据。
- [x] **P0 云函数现状契约**：21 个有效云函数已逐一记录调用来源、主要输入、读取/写入集合、当前返回和身份边界；自动检查保证源码目录与契约双向一致，并锁定 3 个定时任务。本轮不改变云函数运行逻辑。
- [x] **P0 浏览计数云端边界**：`look` 仅接受 `ss/tianmeizhoubian/tj` 三种帖子类型和非空帖子 ID，数据库更新改为等待完成后返回稳定结果；普通帖 `+1/+6`、周边帖 `+7`、留言 `+1` 的旧计数规则保持不变。
- [x] **P0 投票云端一致性**：新版投票由 `VoteOption` 使用可信 OpenID 在事务内校验选项归属和个人票数上限，并同时写投票记录、选项票数和帖子票数；旧客户端协议继续保留，避免云函数先部署导致在线版本无法计数。
- [x] **P0 点赞云端身份边界**：`dianzan` 从可信 OpenID 反查实际用户文档 ID，不再采用前端传入的点赞人 ID；普通帖、周边帖和评论点赞均限制集合范围、等待事务写入完成并返回点赞状态，现有消息字段和点击切换体验保持不变。
- [x] **P0 评论人可信身份**：`fbpl`、`fbzbpj` 在写入前使用云端 OpenID 反查用户，覆盖前端传入的评论人 ID，并由云端生成评论时间和评论 ID；普通帖、周边帖、留言及现有回复通知字段保持兼容。
- [x] **P0 评论删除云端鉴权**：`delete` 根据可信 OpenID、帖子作者、评论/回复作者及 `system01.system.glids` 管理员列表判权，只允许操作三个既有帖子集合；删除和评论计数在事务内同步完成并返回明确结果。
- [x] **P0 管理能力云端鉴权**：`jubaoplus`、`update_post_status` 及 `getworkmessage` 手动入口统一读取 `system01.system.glids` 验证管理员；定时任务继续允许无用户上下文运行，审核状态限制为既有的通过/拒绝值。
- [x] **P0 举报人可信身份**：`jubao` 使用云端 OpenID 反查举报人，不再信任前端 `jbrid`；同一用户重复举报和已达举报上限均返回明确错误，不重复增加计数。
- [x] **P0 通知发送边界**：`ordernotice` 从可信接单人和帖子数据确定发送者、接收者与标题，不再采用前端楼主身份；`checknotice` 仅允许管理员调用且接收名单来自系统配置。
- [x] **P1 云函数错误适配试点**：新增统一云函数调用与错误文案映射，投票及两类详情评论可识别未登录、无权限、重复操作、对象不存在和网络失败，并保证提交锁在成功或失败后释放。
- [x] **P1 互动防重复提交**：普通详情投票与普通/周边评论增加页面级提交锁，连续点击只产生一次在途云函数请求，失败后恢复可操作状态。
- [x] **P2 `plate2` 第一批拆分**：投票提交、错误提示和防重复逻辑已从详情页抽离到 `utils/plate2-vote.js`，页面方法和 WXML 绑定保持不变。
- [x] **P2 `plate2` 评论交互拆分**：评论菜单、回复/复制/删除分发、展开收起、评论云函数提交和输入同步已迁移到 `utils/plate2-comments.js`；Page 方法名与 WXML 绑定保持不变。
- [x] **P2 `plate2` 分享模块拆分**：好友分享、朋友圈分享和复制逻辑已迁移到 `utils/plate2-share.js`；新旧分享字段、派单参数、标题和首图规则保持不变。
- [x] **P2 `plate2` 工程边界拆分完成**：图片选择/压缩/审核、帖子读取/历史记录、派单举报管理、页面加载与滚动生命周期分别迁移到 `plate2-images/data/management/lifecycle.js`；加上既有投票、评论和分享模块，主页面由 2800 余行降至 2000 行以内，Page 方法名和 WXML 绑定不变。
- [x] **P1 发帖个人资料可选入口统一**：首页和工具页发布入口移除手机号、昵称、性别、头像和专业必填拦截，仅保留登录与封禁检查；派单帖仍保留电话或微信至少一种联系方式的业务校验。
- [x] **P1 评论图片选择失败反馈**：普通详情评论图片选择补充失败和权限提示，用户取消保持静默，相册/相机权限或系统调用失败不再表现为按钮无反应。
- [x] **P1 评论发送前移除重复的 `users` 集合直读**：复用登录态中的 `pinglunguode`，避免受限环境输出数据库权限错误；评论写入仍由 `fbpl` 云函数处理。
- [x] **P0 普通发帖写入收口云函数**：新增 `publishPost`，统一创建 `ss`、投票选项和用户发帖记录，并以云端登录身份校正作者字段；前端不再直接写入三个集合。
- [x] **P0 发帖事务与失败反馈**：普通帖和派单帖继续共用 `ss` 及现有页面体验；`publishPost` 通过事务保证帖子、投票和用户记录全成功或全回滚，保留“准备发送/就快好了/即将完成”提示，文字审核、图片审核、图片上传和网络失败分别反馈且失败不清空当前内容。
- [x] **P1 带图发帖审核提速**：文字审核与图片审核改为并行等待，多张图片继续并行审核、并行上传；普通帖/派单帖表单、`ss` 数据结构、失败保留内容及三段发布提示不变。
- [x] **P1 受限权限下投票组件恢复**：普通详情不再直接读取 `VoteOption/VoteRecord`，复用现有 `VoteOption` 云函数一次返回公开选项和当前用户投票状态；不新增云函数，投票展示和交互字段保持不变。
- [x] **P1 受限权限下码住恢复**：普通详情不再直接修改 `ss.ss_xx.Mazhu`，复用已有 `dianzan` 云函数按可信身份事务切换码住状态；补充重复点击保护、失败提示和自己的帖子提示，不新增云函数。
- [x] **P0 受限权限下帖子管理恢复**：普通详情的内容修改、活动结束/恢复、派单结束/恢复和整帖删除不再由前端直接写 `ss/users`；复用已有 `delete` 云函数校验楼主/管理员，整帖与个人记录事务删除，按钮、弹窗和成功文案保持不变。
- [x] **P1 活动状态幂等保存**：活动与派单状态由前端明确提交目标布尔值，云函数执行确定性设置，不再仅依赖云端旧值取反；刷新、重试或重复请求不会把已结束状态意外切回。
- [x] **P0 核心客户端写入收口**：周边发布/编辑、接单、个人删帖、审核删除、管理员封禁及工作队列操作复用现有云函数执行身份校验和写入；失败时不再先显示成功。
- [x] **P1 当前用户写入收口**：新用户建档、在线/登录时间、资料、手机号、订阅计数、浏览/搜索历史及消息删除改由可信 OpenID 定位用户的现有云函数处理；非周边详情页已无客户端数据库直写。
- [x] **P1 周边详情页写入收口**：内容编辑、评分汇总、订阅计数、下载计数、删帖、活动/派单状态和接单已复用 `delete/fbzbpj/login/look/ordernotice` 云函数；页面不再直接写数据库，原有云资源 URL 测试改动保留且不混入本次 Git 提交。
- [x] **P1 帖子定位字段契约统一**：消息、登录返回及主要内部入口统一生成 `postId/postType/commentId/replyId/source/schoolId`；留言 `tj` 归一到所属详情页并自动设置 `liuyan=true`，回复优先定位 `replyId`，旧字段继续兼容。
- [x] **P0 数据库运维基线**：基于实际 `where/orderBy/skip/limit` 建立索引优先级，记录可立即收紧与需迁移后收紧的权限矩阵，定义带计数和 SHA-256 的备份清单、空环境恢复顺序及回滚条件；本步不直接改动云端。
