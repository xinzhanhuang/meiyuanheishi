# 天美黑市小程序开发指南

## 项目总结

通过对项目的全面分析,这是一个**功能完整、架构清晰的校园社区小程序**,充分利用了微信云开发的能力,实现了发帖、评论、点赞、消息推送等核心社交功能。

---

## 核心技术要点

### 1. 云开发架构

#### 云数据库操作模式
```javascript
// 标准查询模式
const db = wx.cloud.database()
const _ = db.command

// 查询帖子
db.collection('ss').where({
  'ss_xx.choosetitle': '话题名称'
}).orderBy('ss_xx.firsttime', 'desc').get()

// 更新操作(增量)
db.collection('ss').doc(id).update({
  data: {
    'ss_xx.dianzannb': _.inc(1),  // 数字增加
    'ss_xx.dianzanid': _.push(userId)  // 数组添加
  }
})

// 删除数组元素
db.collection('ss').doc(id).update({
  data: {
    'ss_xx.dianzanid': _.pull(userId)
  }
})
```

#### 实时数据监听
```javascript
// 监听用户消息变化
this.watcher = db.collection('users').doc(_id).watch({
  onChange: (snapshot) => {
    // 处理数据变化
    console.log('数据更新:', snapshot)
    // 更新红点提示
    this.updateRedDot()
  },
  onError: (err) => {
    console.error('监听错误:', err)
  }
})

// 记得在页面卸载时关闭监听
onUnload() {
  if (this.watcher) {
    this.watcher.close()
  }
}
```

### 2. 图片处理技术

#### Canvas压缩方案
```javascript
// 压缩流程
async yasuo(media, quality, maxSize) {
  // 1. 获取图片信息
  const imgInfo = await wx.getImageInfo({ src: media })
  
  // 2. 计算压缩尺寸(保持比例)
  let width = imgInfo.width
  let height = imgInfo.height
  if (width > height) {
    height = Math.trunc(maxSize * height / width)
    width = maxSize
  } else {
    width = Math.trunc(maxSize * width / height)
    height = maxSize
  }
  
  // 3. Canvas绘制
  const ctx = wx.createCanvasContext('canvas-id')
  ctx.drawImage(media, 0, 0, width, height)
  
  // 4. 导出压缩图片
  ctx.draw(false, () => {
    wx.canvasToTempFilePath({
      canvasId: 'canvas-id',
      destWidth: width,
      destHeight: height,
      quality: quality,  // 0.92 = 高质量, 0.5 = 审核用
      success: (res) => {
        console.log('压缩完成:', res.tempFilePath)
      }
    })
  })
}
```

#### 图片审核策略
```javascript
// 两次压缩策略
// 1. 高质量压缩(0.92) - 用于上传显示
// 2. 低质量压缩(0.5, 400px) - 用于内容审核

async imgcheck() {
  const imgs = this.data.imgs
  const displayImgs = []  // 显示用
  const checkImgs = []    // 审核用
  
  // 第一次压缩 - 显示用
  for (let img of imgs) {
    const compressed = await this.yasuo(img, 0.92, 1000)
    displayImgs.push(compressed)
  }
  
  // 第二次压缩 - 审核用
  for (let img of imgs) {
    const compressed = await this.yasuo(img, 0.5, 400)
    checkImgs.push(compressed)
  }
  
  // 审核检测
  for (let img of checkImgs) {
    const buffer = await this.qubuffer(img)
    const result = await this.checkImg(buffer)
    if (result !== 0) return false
  }
  
  return displayImgs
}
```

### 3. 评论系统设计

#### 评论数据结构
```javascript
// 评论对象
const comment = {
  pinglunID: Date.now().toString(),  // 评论ID
  plrid: userId,                      // 评论人ID
  name: username,                     // 评论人昵称
  photo: userPhoto,                   // 评论人头像
  wbnr: content,                      // 评论内容
  tp: images,                         // 评论图片
  time: Date.now(),                   // 评论时间
  dianzhanID: [],                     // 点赞用户列表
  pldianzannb: 0,                     // 点赞数
  huifu: [],                          // 回复列表
  huifunb: 0                          // 回复数
}
```

#### 评论/回复云函数
```javascript
// 调用评论云函数
wx.cloud.callFunction({
  name: 'fbpl',
  data: {
    pinglunnr: comment,
    pd: [isFirstComment, replyToId, replyTime],
    Mazhu: collectUserIds  // 收藏者ID列表
  }
})

// pd参数说明:
// pd[0]: 是否首次评论该帖子
// pd[1]: 回复的评论ID(空表示评论帖子)
// pd[2]: 回复的评论时间
```

### 4. 订阅消息推送

#### 授权流程
```javascript
// 请求订阅授权
wx.requestSubscribeMessage({
  tmplIds: ['模板ID'],
  success: (res) => {
    if (res['模板ID'] === 'accept') {
      // 用户同意授权
      this.updateUserSubscription(true)
    }
  }
})
```

#### 云函数发送
```javascript
// 在云函数中发送订阅消息
cloud.openapi.subscribeMessage.send({
  touser: openid,
  page: 'pages/plate2/plate2?id=' + postId,
  data: {
    thing17: { value: '被回复的内容' },
    thing3: { value: '评论人' },
    thing8: { value: '评论内容' }
  },
  templateId: 'hs60e8rl8z2e_dMIRqBgT5izdt7qw_e9O3Y8xWFh9pY',
  miniprogramState: 'formal'
})
```

### 5. 投票功能实现

#### 投票数据模型
```javascript
// 帖子表中的投票字段
{
  voteNumberPerPerson: 1,  // 每人可投票数
  voteOption: ['选项A', '选项B', '选项C'],
  votepeopleNumber: 0,     // 已投票人数
  isEnd: false             // 是否结束
}

// VoteOption表(单独记录)
{
  id: postId,              // 关联帖子ID
  voteOption: '选项A',
  voteNumber: 10           // 该选项票数
}

// VoteRecord表(投票记录)
{
  postId: postId,
  userId: userId,
  options: ['选项A']       // 已投选项
}
```

---

## 开发流程指南

### 新增功能页面

1. **创建页面文件**
```bash
# 在 miniprogram/pages 下创建
pages/
  new-feature/
    new-feature.js
    new-feature.json
    new-feature.wxml
    new-feature.wxss
```

2. **注册页面路由**
```json
// app.json
{
  "pages": [
    "pages/new-feature/new-feature"
  ]
}
```

3. **页面生命周期**
```javascript
Page({
  data: {},
  
  onLoad(options) {
    // 页面加载,获取参数
    const id = options.id
  },
  
  onShow() {
    // 页面显示,刷新数据
    if (app.shuaxin) {
      this.loadData()
      app.shuaxin = false
    }
  },
  
  onUnload() {
    // 页面卸载,关闭监听
    if (this.watcher) {
      this.watcher.close()
    }
  }
})
```

### 新增云函数

1. **创建云函数**
```bash
# 在 cloudfunctions 下创建
cloudfunctions/
  my-function/
    index.js
    package.json
```

2. **云函数代码**
```javascript
// index.js
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })

exports.main = async (event, context) => {
  const wxContext = cloud.getWXContext()
  const db = cloud.database()
  
  try {
    // 业务逻辑
    const result = await db.collection('users')
      .doc(event.userId)
      .get()
    
    return {
      success: true,
      data: result.data
    }
  } catch (err) {
    return {
      success: false,
      error: err
    }
  }
}
```

3. **上传部署**
- 右键云函数文件夹
- 选择"上传并部署:云端安装依赖"

4. **小程序调用**
```javascript
wx.cloud.callFunction({
  name: 'my-function',
  data: { userId: 'xxx' }
}).then(res => {
  console.log(res.result)
})
```

---

## 常见问题解决

### 1. 图片加载慢

**问题**: 帖子列表图片加载缓慢

**解决方案**:
```javascript
// 使用图片预加载标记
data: {
  'ss_xx.tp2': [
    { loaded: false },
    { loaded: false }
  ]
}

// 图片加载完成回调
imageOnLoad2(e) {
  const index0 = e.currentTarget.dataset.index0
  const index1 = e.currentTarget.dataset.index1
  this.setData({
    [`ss_xx[${index0}].ss_xx.tp2[${index1}].loaded`]: true
  })
}

// WXML中使用
<image 
  hidden="{{!item.ss_xx.tp2[0].loaded}}"
  src="{{item.ss_xx.tp[0]}}"
  bindload="imageOnLoad2"
/>
<image 
  wx:if="{{!item.ss_xx.tp2[0].loaded}}"
  src="/images/loading8.gif"
/>
```

### 2. 消息红点不更新

**问题**: TabBar红点不显示

**解决方案**:
```javascript
// 在需要的页面启动监听
onShow() {
  if (!app.jianting) {
    this.jianting()
    app.jianting = true
  }
}

// 监听用户消息变化
jianting() {
  const _id = app.userInfo._id
  this.watcher = db.collection('users').doc(_id).watch({
    onChange: (snapshot) => {
      const message = snapshot.docs[0].message
      const unreadCount = message.filter(m => !m.read).length
      
      if (unreadCount > 0) {
        wx.setTabBarBadge({
          index: 2,  // 消息tab索引
          text: unreadCount.toString()
        })
      } else {
        wx.removeTabBarBadge({ index: 2 })
      }
    }
  })
}
```

### 3. 数据权限问题

**问题**: 用户只能读取自己的数据

**解决方案**:
```javascript
// 使用云函数绕过权限限制
// 云函数中可以访问所有数据
exports.main = async (event, context) => {
  const db = cloud.database()
  
  // 云函数有完整权限
  const result = await db.collection('users')
    .where({ _openid: event.targetOpenid })
    .get()
  
  return result
}
```

### 4. 发帖后不刷新

**问题**: 发帖成功后首页不更新

**解决方案**:
```javascript
// 发帖页面设置刷新标记
post() {
  db.collection('ss').add({ data: postData })
    .then(() => {
      app.shuaxin = true  // 设置全局刷新标记
      wx.switchTab({ url: '/pages/index/index' })
    })
}

// 首页检测刷新标记
onShow() {
  if (app.shuaxin) {
    this.jiazai(true)  // 重新加载数据
    app.shuaxin = false
  }
}
```

---

## 代码优化建议

### 1. 抽取公共方法

**当前问题**: 图片压缩、审核代码在多个页面重复

**优化方案**:
```javascript
// utils/imageUtil.js
module.exports = {
  // 图片压缩
  async compressImage(path, quality, maxSize) {
    // 压缩逻辑
  },
  
  // 图片审核
  async checkImage(buffer) {
    return wx.cloud.callFunction({
      name: 'checkImg',
      data: { media: buffer }
    })
  },
  
  // 完整流程
  async processImages(images) {
    const displayImgs = []
    for (let img of images) {
      const compressed = await this.compressImage(img, 0.92, 1000)
      const buffer = await this.getBuffer(compressed)
      const checkResult = await this.checkImage(buffer)
      if (checkResult !== 0) throw new Error('审核失败')
      displayImgs.push(compressed)
    }
    return displayImgs
  }
}
```

### 2. 统一错误处理

```javascript
// utils/errorHandler.js
module.exports = {
  handle(error, context = '') {
    console.error(`[${context}]`, error)
    
    let message = '操作失败,请重试'
    if (error.errCode === -604102) {
      message = '图片包含违规内容'
    } else if (error.errCode === 87014) {
      message = '文本包含敏感信息'
    }
    
    wx.showToast({
      title: message,
      icon: 'none',
      duration: 2000
    })
  }
}
```

### 3. 数据缓存优化

```javascript
// 使用本地缓存减少请求
async loadUserInfo() {
  // 先从缓存读取
  const cached = wx.getStorageSync('userInfo')
  if (cached && Date.now() - cached.timestamp < 3600000) {
    return cached.data
  }
  
  // 缓存过期,重新获取
  const result = await db.collection('users')
    .doc(userId)
    .get()
  
  // 更新缓存
  wx.setStorageSync('userInfo', {
    data: result.data,
    timestamp: Date.now()
  })
  
  return result.data
}
```

---

## 安全注意事项

### 1. 内容审核必须在云端

❌ **错误做法**: 小程序端直接上传
```javascript
// 不安全 - 用户可以绕过审核
db.collection('ss').add({ data: postData })
```

✅ **正确做法**: 云函数审核后上传
```javascript
// 云函数中审核
const checkResult = await checkContent(text, images)
if (!checkResult.pass) {
  return { success: false, reason: '内容违规' }
}
// 审核通过才写入
await db.collection('ss').add({ data: postData })
```

### 2. 敏感操作验证权限

```javascript
// 删除帖子前验证
async deletePost(postId) {
  const post = await db.collection('ss').doc(postId).get()
  const userId = app.userInfo._id
  
  // 验证是否是作者或管理员
  if (post.data.ss_xx.lzid !== userId && 
      !app.glids.includes(userId)) {
    throw new Error('无权删除')
  }
  
  // 执行删除
  await db.collection('ss').doc(postId).remove()
}
```

### 3. 防止恶意刷量

```javascript
// 点赞防抖
let lastLikeTime = 0
dianzan(e) {
  const now = Date.now()
  if (now - lastLikeTime < 1000) {
    wx.showToast({ title: '操作太频繁', icon: 'none' })
    return
  }
  lastLikeTime = now
  
  // 执行点赞
  this.performLike(e)
}
```

---

## 性能优化清单

- ✅ 图片懒加载 (`lazy-load="true"`)
- ✅ 列表分页加载 (`onReachBottom`)
- ✅ 数据库查询限制 (`.limit(20)`)
- ✅ 图片压缩上传
- ⚠️ 建议添加: 虚拟列表(长列表优化)
- ⚠️ 建议添加: 图片CDN加速
- ⚠️ 建议添加: 数据库索引优化

---

## 项目亮点

1. **完整的社交功能**: 发帖、评论、点赞、收藏、消息推送
2. **实时互动体验**: 消息监听、在线状态、红点提示
3. **严格的内容审核**: 文本+图片双重审核机制
4. **灵活的扩展功能**: 投票、派单、周边点评
5. **良好的用户体验**: 图片预览、搜索、排序、刷新

---

## 下一步建议

### 功能增强
- [ ] 添加话题标签系统
- [ ] 实现用户关注功能
- [ ] 增加私信功能
- [ ] 添加帖子草稿箱
- [ ] 实现内容推荐算法

### 技术优化
- [ ] 引入状态管理(如Mobx)
- [ ] 组件化重构
- [ ] 添加单元测试
- [ ] 性能监控埋点
- [ ] 错误日志上报

### 运营支持
- [ ] 数据统计面板
- [ ] 用户行为分析
- [ ] 内容质量评分
- [ ] 自动化运营工具

---

**项目已完全熟悉,可以开始任何开发工作!** 🎉
