# 学校上下文子系统开发记录

## 本轮交付

- [x] 统一 `get/list/set/refresh` 入口，复用现有 `config/schools.js` 与 `services/school-service.js`。
- [x] `set` 安全切换：更新 `app.currentSchoolId`、持久化当前学校，并清理学校相关首页/列表缓存。
- [x] 增加最小可取消订阅机制，切校后向未来页面发送上下文快照。
- [x] `refresh` 保留本地 `tjarts` 兜底、Promise 去重和错误收口，不阻塞单校启动。
- [x] 不自动切校、不启用查询过滤、不改页面 UI、不改数据库、不新建云函数或依赖。
- [x] 通过 `scripts/check-school-context-subsystem.js` 行为检查。

## 缓存边界

当前仅清理约定的 `zuiress_xx1`、`zilei`、`schoolHomeCache`、`schoolListCache` 及对应保留键；登录、徽标和待恢复分享状态不属于学校缓存，切校时保留。

## 后续入口

页面需要响应学校变化时，使用 `app.subscribeSchoolContext(listener)`；在学校选择 UI 和查询过滤正式启用前，不接入页面。
