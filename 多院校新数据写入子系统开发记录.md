# 多院校新数据写入字段子系统开发记录

## 本轮范围

在不启用查询过滤、不迁移旧数据、不写入云数据库的前提下，为可信的新增文档写入路径补齐 `schoolId`。普通帖子、投票和搜索由云函数生成，不能信任前端伪造值；现阶段统一回退 `tjarts`（天津美术学院）。

## 写入路径审计

| 集合 | 新增入口 | 处理结果 |
| --- | --- | --- |
| `users` | `cloudfunctions/login` 的 `ensureUser` | 新用户写入 `schoolId: tjarts`；旧用户更新不做迁移 |
| `ss` | `publishPost`、`getworkmessage` | 普通帖子取当前浏览学校，并由云函数确认该学校为 active；自动聚合内容使用 `tjarts` |
| `tj` | 无顶层新增；`fbpl/fbzbpj` 仅更新既有主文档 | 不改旧评论协议，记录为待后续主文档建模 |
| `VoteOption` | `publishPost` | 与新帖子使用同一云端学校值 |
| `VoteRecord` | `VoteOption` v2 事务 | 从已读取的 `ss.schoolId` 生成，缺失时回退 `tjarts` |
| `searchLogs` | `login` 的 `recordSearch` | 从已认证用户记录生成 |
| `search_keywords` | `aggregateSearchKeywords` | 新增关键词记录补 `tjarts`；既有记录更新不回写字段 |
| `work_queue` | `getnewlines` | 抓取任务在服务端生成 `tjarts` |

## 安全与兼容边界

- `users.schoolId` 表示默认主学校，`currentSchoolId` 表示当前浏览/发帖学校。`publishPost` 仅接受经云端 `schools` 集合确认为 active 的当前学校；单校过渡期 `tjarts` 始终可用。周边旧协议保持原样。
- 旧客户端投票计数协议继续保留；新 `actionVersion: 2` 记录补字段。
- 旧数据、旧查询和旧分享协议未改；没有执行迁移、部署或真实数据库写入。

## 验收

- [x] `scripts/check-multischool-write-subsystem.js` 静态检查通过。
- [x] 各目标集合新增路径已登记；`tj` 无顶层新增路径已确认。
- [x] 未新增依赖，未修改周边集合逻辑。
