# 多院校路由、分享、消息与登录返回子系统

## 本轮完成

- `getPostTarget` 对缺少 `schoolId` 的旧链接回退 `tjarts`。
- `getPostTargetUrl` 始终携带 `schoolId`，保留 `postId`、`postType`、`commentId`、`replyId`、`source`、`liuyan` 与旧 `id` 参数。
- 普通帖子和周边详情进入时恢复目标学校上下文；不启用学校数据过滤或选校 UI。
- 普通帖子分享路径携带 `schoolId`；周边分享复用统一目标 URL。
- 登录待返回目标保存学校上下文，登录后继续使用统一目标 URL 返回详情。
- 消息页继续读取 `postId` / `ssid`、`postType`、`commentId`、`replyId`、`schoolId`，评论定位和高亮逻辑不变。
- 评论、点赞、派单、举报和审核通知的 service/云函数边界补传并写入 `schoolId`；订阅消息页路径同步携带学校参数。
- 周边拒绝通知的旧编辑链接保持原样兼容。

## 检查

```bash
node scripts/check-school-route-share.js
node scripts/check-post-target.js
node scripts/check-login-return.js
node scripts/check-message-targets.js
node scripts/check-share-targets.js
node scripts/check-shared-post-login-return.js
```

本轮未修改周边新功能、数据库内容、用户脏文件，也未执行 commit/push。
