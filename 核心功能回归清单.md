# 天美黑市核心功能回归清单

## 使用方法

- 每次优化只执行与改动相关的章节，涉及公共逻辑时执行所有受影响章节。
- 每项记录“通过 / 失败 / 未验证”，不能将“未验证”当作“通过”。
- 优化前后使用同一账号、同一帖子和同一操作顺序对比。
- 涉及真实数据写入的测试，优先使用专用测试账号和可删除的测试内容。
- 验证失败时停止后续优化，先确认是否由本次改动引起。

## 统一测试前置与待验证汇总

以下事项只记录为“待验证”，在最后统一测试前不得打勾或宣称线上可用。

### 云环境前置

- [ ] 在测试环境部署当前分支涉及的云函数，至少包括 `publishPost`、`jubao`、`jubaoplus`；其余以最终 Git 差异和云函数台账为准。
- [ ] 在测试环境确认 `ss(authorId, requestId)` 复合索引可用。
- [ ] 在测试环境确认 `tianmeizhoubian(authorId, requestId)` 复合索引可用。
- [ ] 按《数据库索引权限与备份恢复清单》确认 P0 索引、集合权限、数据数量和回滚值。
- [ ] 确认测试环境的隐私保护指引已声明头像昵称、相册和相机等实际使用能力。

### 本轮架构改造专项

- [ ] 新发布普通帖顶层存在 `postType/schoolId/authorId/status/createdAt/updatedAt/requestId`，旧 `ss_xx` 字段和页面展示不变。
- [ ] 普通帖和派单帖仍写入 `ss`，但 `postType` 分别正确记录为 `post/order`。
- [ ] 同一次普通帖发布请求超时后重试，不创建重复帖子或重复投票选项。
- [ ] 同一次周边发布请求超时后重试，不创建重复内容或重复个人记录。
- [ ] 发布成功、审核失败、图片上传失败和网络失败时，提示、草稿保留和 `publishState` 正确。
- [ ] 评论云函数失败时不清空输入、不增加本地评论数、不显示“评论成功”。
- [ ] 删除评论失败时不先从界面移除；成功后评论和计数一致。
- [ ] 帖子点赞、评论点赞和码住快速连点不会产生重复写入或本地/云端不一致。
- [ ] 投票快速连点、超额投票和重复投票由云端拒绝，失败后仍可继续正常操作。
- [ ] 第 10 次普通举报时，帖子举报数、楼主违规次数、个人发帖违规标记和消息在同一事务中完成。
- [ ] 举报事务失败时上述数据均不产生部分写入，页面不显示成功。
- [ ] 管理员封帖对 `ss` 和 `tianmeizhoubian` 均能完整更新，非管理员调用被拒绝。
- [ ] 分享入口、登录返回、留言模式和评论定位在 service 迁移后保持不变。
- [ ] 周边详情的加载、下载计数、评分、编辑、删除、活动/派单状态、接单、举报、帖子点赞和评论点赞在 service 迁移后结果不变。
- [ ] 周边评论发布/删除失败时不提前改变列表、计数或输入内容，成功时订阅提示和评分更新正常。

### 历史待统一验证

- [ ] 数据库权限收紧后不出现新的 `-502003`、页面空白或直写失败。
- [ ] 实时监听离线、鉴权失败、前后台切换和重连不会产生重复 watcher 或未处理异常。
- [ ] 无头像、昵称、手机号和专业的新用户可登录、发普通帖和评论。
- [ ] 评论图片在隐私声明正确时可选择、审核、上传和显示。
- [ ] 消息点击进入帖子后能定位并短暂高亮目标评论/回复，高亮随后消失。
- [ ] 普通详情和周边详情的活动结束、恢复、修改、删除、接单及刷新后状态正确。
- [ ] 投票组件只在有投票的帖子显示，老帖子和无投票帖子详情不报错。
- [ ] 首页、分类、搜索、热帖和周边列表的分页、排序、刷新与增量处理没有重复数据。

## 1. 启动与全局状态

- [ ] 小程序可正常编译并进入首页。
- [ ] 首页、点评、我的三个 Tab 名称、图标、顺序和选中颜色不变。
- [ ] 前台进入时用户 `online` 状态正常更新。
- [ ] 进入后台时用户 `online` 状态正常更新。
- [ ] 新版本检测和更新提示逻辑正常。
- [ ] 页面切换后没有重复 watcher、重复请求或异常报错。

## 2. 未登录与登录

- [ ] 未登录用户可浏览允许访问的首页和帖子内容。
- [ ] 未登录时执行发帖、点赞、评论等受限操作，提示和跳转与基线一致。
- [ ] 首次登录能正常创建 `users` 记录。
- [ ] 已登录用户能正常恢复用户信息。
- [ ] 用户头像、昵称、性别、手机号和专业等字段显示正常。
- [ ] 封禁用户无法执行原本受限的操作。

## 3. 首页

- [ ] 轮播图、公告和分类入口正常显示。
- [ ] 最新/最热 Tab 切换结果和排序规则不变。
- [ ] 帖子文本、头像、图片、标签和统计数显示正常。
- [ ] 单图、多图、无图帖子布局不变。
- [ ] 图片加载失败时不会导致页面卡住。
- [ ] 上拉加载没有重复数据，到底状态正确。
- [ ] 下拉刷新后列表恢复到首页，没有残留旧状态。
- [ ] 点击帖子、头像、标签和图片的跳转/预览结果不变。

## 4. 搜索与分类

- [ ] 搜索词输入、清空和提交正常。
- [ ] 搜索建议能正常显示并跳转。
- [ ] 热门搜索词和标签帖子数正常。
- [ ] 搜索无结果时空状态正常。
- [ ] 搜索结果的排序、分页和详情跳转不变。
- [ ] 分类页的帖子范围与基线一致。

## 5. 普通发帖

- [ ] 纯文字帖子可正常发布。
- [ ] 含图帖子可正常选图、压缩、审核、上传和发布。
- [ ] 图片数量上限、删除和顺序不变。
- [ ] 文字和图片审核未被绕过，违规内容提示正常。
- [ ] 匿名开关的写入和展示结果不变。
- [ ] 位置选择、取消和展示正常。
- [ ] 投票选项、每人票数、截止状态和发布结果不变。
- [ ] 重复点击发布不会创建多条数据。
- [ ] 发布失败时用户输入和已选图片不会意外丢失。

## 6. 周边发布与点评

- [ ] 点评 Tab 各分类列表正常。
- [ ] 综合/评分等排序方式结果不变。
- [ ] 周边内容的文字、图片、位置和分类发布正常。
- [ ] 评价数、评分和点赞数更新正常。
- [ ] 无图、单图和多图内容显示正常。
- [ ] 列表加载、刷新和分类切换没有重复数据。

## 7. 帖子详情与浏览

- [ ] 从首页、搜索、分类、消息和分享链接进入详情均正常。
- [ ] 帖子不存在或无权访问时提示正常。
- [ ] 浏览量只按基线规则增加，不会因重复加载异常增长。
- [ ] 图片预览、位置打开、文字复制和分享正常。
- [ ] 帖主、接单人和管理员所见操作入口与基线一致。
- [ ] 普通帖和周边帖详情各自读写正确的数据集。

## 8. 点赞与收藏

- [ ] 首次点赞后状态、数量和数据库点赞用户列表正确。
- [ ] 取消点赞后状态、数量和数据库结果正确。
- [ ] 快速重复点击不会导致负数、重复 ID 或界面与云端状态不一致。
- [ ] 帖子点赞和评论点赞不会互相污染。
- [ ] 普通帖和周边帖点赞写入正确的数据集。
- [ ] 对他人内容点赞时消息记录正常，对自己内容操作不产生错误通知。
- [ ] 收藏和取消收藏后界面与数据库状态一致。

## 9. 评论与回复

- [ ] 纯文字评论和回复可正常发送。
- [ ] 含图评论可正常审核、上传和显示。
- [ ] 评论人、被回复人、帖主和时间等字段正确。
- [ ] 一级评论和子回复计数正确。
- [ ] 新旧 `pinglunID` 兼容逻辑正常。
- [ ] 删除自己的评论/回复后列表和计数正确。
- [ ] 帖主和管理员的删除权限与基线一致。
- [ ] 内容审核失败或网络失败时不会误增评论数。
- [ ] 输入框聚焦、回复对象、键盘收起和页面滚动体验不变。

## 10. 投票与派单

- [ ] 未投票用户能在限额内选择选项。
- [ ] 投票后选项票数、参与人数和用户记录正确。
- [ ] 重复投票和超出每人票数限制时行为不变。
- [ ] 投票结束后不能继续投票。
- [ ] 接单资格检查、确认提示和写入结果正确。
- [ ] 帖主和接单人的订单状态与操作入口正确。
- [ ] 订单完成/结束后不能被重复处理。
- [ ] 派单通知的接收人、内容和跳转目标正确。

## 11. 消息与红点

- [ ] 评论、回复、点赞和系统消息分类正确。
- [ ] 新消息到达时 watcher 能更新页面和 TabBar 红点。
- [ ] 查看消息后已读状态和红点正常清除。
- [ ] 重复进出首页、我的和消息页不会创建多个 watcher。
- [ ] 首页与我的页面快速切换时，两页的消息和红点订阅不会互相覆盖。
- [ ] 断网进入首页时公共帖子加载不被用户会话阻塞，恢复网络后用户和红点可恢复。
- [ ] 我的页面登录、重复进入和下拉刷新后，头像、昵称、帖子、评论/点赞消息不回退为旧值。
- [ ] 从首页和我的页面进入普通帖、留言帖及登录返回帖子时，路由和评论定位保持正确。
- [ ] 点击各类消息可跳转到正确帖子或功能页。
- [ ] 目标帖子已删除时有正常提示，不会卡在加载状态。

## 12. 举报、审核与管理

- [ ] 普通用户能按原有流程举报内容。
- [ ] 重复举报、举报自己和举报管理员等边界行为不变。
- [ ] 非管理员无法进入或执行管理操作。
- [ ] 管理员列表从 `system` 配置正常获取。
- [ ] 审核列表加载、通过、拒绝和状态刷新正常。
- [ ] 管理删除不会误删其他帖子、评论或云存储文件。

## 13. 分享与异常场景

- [ ] 普通帖、周边帖和轮播内容分享后路由参数正确。
- [ ] 从分享链接冷启动时，未登录和已登录用户都能正确进入目标页。
- [ ] 未注册用户从普通/周边分享帖点击评论、点赞等受限操作，完成登录后返回原帖；网络失败后重试仍保留目标。
- [ ] 已注册用户冷启动打开普通/周边分享帖时自动恢复会话，无需再点登录，消息 watcher 和红点随后正常。
- [ ] 无网络、慢网络和云函数失败时有可恢复的界面状态。
- [ ] 数据集返回空数组或字段缺失时页面不崩溃。
- [ ] 快速连续点击、切换 Tab 和返回页面时不会重复写入数据。
- [ ] 小程序切到后台再恢复时，页面和用户状态正常。

## 单次回归记录模板

```text
任务：
分支/Commit：
验证环境：开发者工具 / 预览 / 真机 / 云环境
测试账号：

已通过：
-

失败：
-

未验证：
-

数据变更及清理情况：
-

结论：可提交 / 不可提交
```
