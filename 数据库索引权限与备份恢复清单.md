# 数据库索引、权限与备份恢复清单

> 依据：2026-08-29 仓库中的实际查询和云函数边界。本清单不直接修改云端环境；正式环境配置前先在测试环境逐项验证。

## 1. 执行原则

1. 先完整备份数据、权限、索引、云函数环境变量和触发器，再改权限或索引。
2. 只为代码中已存在的查询建索引；不为 `_id` 单文档查询重复建索引。
3. 客户端读取仍在使用的集合，不能一次改成“仅云函数可读”；否则会再次出现 `-502003` 和页面空白。
4. 写权限以云函数为边界；任何临时放开写入都必须有明确的回收时间。
5. 正式环境权限改动每次只改一个集合，回归通过后再继续。

## 2. 索引清单

字段顺序应以云开发控制台实际的“缺少索引”提示为最终依据。先建 P0，P1 只在对应页面查询较慢或控制台要求时创建。

### P0：必须确认

| 集合 | 索引字段 | 方向 | 代码证据/用途 | 状态 |
| --- | --- | --- | --- | --- |
| `users` | `_openid` | 升序 | 所有需鉴权的云函数都按 OpenID 反查用户 | [ ] 控制台确认；已有系统索引则不重建 |
| `VoteOption` | `id` | 升序 | 按帖子 ID 加载投票选项 | [ ] |
| `VoteRecord` | `voteItemId`, `voterId` | 升序、升序 | 检查当前用户是否已投票 | [ ] |
| `ss` | `ss_xx.sstype`, `time` | 升序、降序 | 待审核列表 | [ ] |
| `tianmeizhoubian` | `ss_xx.checked`, `time` | 升序、降序 | 周边审核列表 | [ ] |
| `work_queue` | `url` | 升序 | 抓取任务去重 | [ ] 核对是否允许唯一索引；未验证前先用普通索引 |
| `work_queue` | `created_at` | 降序 | 后台任务列表 | [ ] |
| `lunbotu3` | `schooltype` | 升序 | 首页和周边发布页读取院校轮播图 | [ ] |

### P1：列表组合索引候选

| 集合 | 过滤字段 | 排序字段 | 覆盖场景 | 状态 |
| --- | --- | --- | --- | --- |
| `ss` | `ss_xx.jubao.1`, `time`, `ss_xx.orderdetail.openlocationtitle` | `ss_xx.look desc` | 首页热帖 | [ ] 按控制台提示创建 |
| `ss` | `ss_xx.jubao.1`, `ss_xx.sstype`, `ss_xx.orderdetail.openlocationtitle` | `time desc` | 首页/搜索列表 | [ ] |
| `ss` | `ss_xx.choosetitle`, `ss_xx.jubao.1` | `time desc` | 分类最新 | [ ] |
| `ss` | `ss_xx.choosetitle`, `ss_xx.jubao.1`, `time` | `ss_xx.dianzannb desc` | 分类热门 | [ ] |
| `ss` | `ss_xx.jubao.1`, `time`, `ss_xx.orderdetail.openlocationtitle` | `ss_xx.dianzannb desc` | 热帖页 | [ ] |
| `tianmeizhoubian` | `ss_xx.zilei`, `ss_xx.type`, `ss_xx.checked` | `_createTime desc` | 周边最新 | [ ] |
| `tianmeizhoubian` | `ss_xx.zilei`, `ss_xx.type`, `ss_xx.checked` | `ss_xx.dianzannb desc`, `ss_xx.huifunb desc`, `_createTime desc` | 周边综合排序 | [ ] |
| `tianmeizhoubian` | `ss_xx.zilei`, `ss_xx.type`, `ss_xx.checked` | `downloads/remark_num desc`, `_createTime desc` | 周边下载/评分排序 | [ ] 仅为页面真实启用的排序建索引 |
| `users` | 无 | `logintime desc` | 后台用户列表 | [ ] |

### 不建索引的已知场景

- `ss/tj/system/users` 的 `_id` 单文档定位：使用现有 `_id` 索引。
- 帖子正文、评论、用户名和手机号的 `.*关键词.*` 正则：普通索引无法解决包含搜索，数据量增大后应改用搜索服务或预计算关键词。
- `searchLogs` 的分组聚合：先观察慢查询；可候选建立 `timestamp` 和 `searchText` 单字段索引，不盲目建立大量复合索引。

## 3. 权限目标矩阵

下表表达权限意图，不直接粘贴未验证的规则 JSON。“客户端写=禁止”不影响云函数使用管理端 SDK 写入。

| 集合 | 客户端读 | 客户端写 | 云函数 | 当前是否可立即执行 | 原因/前置任务 |
| --- | --- | --- | --- | --- | --- |
| `ss`, `tianmeizhoubian`, `tj` | 所有用户可读 | 禁止 | 可读写 | 是 | 列表和详情仍由客户端读，所有业务写已收口 |
| `VoteOption`, `VoteRecord` | 禁止 | 禁止 | 可读写 | 是 | 投票加载和提交都已由 `VoteOption` 云函数处理 |
| `searchLogs`, `search_keywords`, `fetch_states` | 禁止 | 禁止 | 可读写 | 是 | 搜索日志与抓取状态不应暴露给客户端 |
| `lunbotu3` | 所有用户可读 | 禁止 | 可读写 | 是 | 客户端只读配置 |
| `system` | 所有用户可读 | 禁止 | 可读写 | 是，但建议后续脱敏 | 页面直读配置；需确认文档中不含密钥、手机号或管理员 OpenID |
| `users` | 已登录用户可读 | 禁止 | 可读写 | 否 | 当前个人页、他人资料和实时监听仍直读整个用户文档；需先拆分公开资料与私密消息 |
| `work_queue` | 管理员可读 | 禁止 | 可读写 | 否 | 管理页仍直接读取，数据库规则无法可靠复用 `system.glids` 管理员判定；应先迁移到 `getworkmessage` |

### 权限变更验收

- [ ] 新用户建档、普通/周边发布、评论、图片评论、点赞、投票、码住和接单通过。
- [ ] 非楼主不能修改/删除帖子，非管理员不能审核或封禁。
- [ ] 列表、详情、分享、留言和历史旧帖仍可读。
- [ ] 客户端尝试直接写入受保护集合必须返回权限拒绝。
- [ ] 每改一个集合后记录时间、环境 ID、规则摘要、操作人和回滚值。

## 4. 备份清单

### 每次权限/索引变更前

- [ ] 按 `YYYYMMDD-HHmm-环境ID-集合名.json` 导出全部集合，保留 CloudBase 原始 JSON 格式。
- [ ] 保存“集合数量 + 每个集合记录数 + 导出文件大小 + SHA-256”清单。
- [ ] 分别保存测试/正式环境的权限规则、索引列表、环境 ID 和数据库地域。
- [ ] 确认 22 个有效云函数源码已在 Git，另行记录已部署版本、Node 版本、内存/超时、环境变量和 3 个定时触发器。
- [ ] 密钥和 Token 只记录“名称、用途、重建方式”，不写入 Git 或数据导出目录。
- [ ] 备份目录另制作一份离线或异地副本，验证可解压和可读。

### 建议目录结构

```text
heishi-backup/YYYYMMDD-HHmm-环境ID/
  manifest.txt
  database/集合名.json
  database/counts.txt
  database/checksums.sha256
  config/permissions/
  config/indexes/
  config/triggers.txt
  config/functions.txt
  config/env-vars-names-only.txt
```

## 5. 恢复演练清单

1. [ ] 在空白测试环境创建集合，暂时保持客户端读写关闭。
2. [ ] 先建 P0 索引，索引就绪后再导入数据。
3. [ ] 依次导入基础配置与用户：`system` → `lunbotu3` → `users`。
4. [ ] 导入业务主数据：`ss` → `tianmeizhoubian` → `tj` → `VoteOption` → `VoteRecord`。
5. [ ] 导入运维数据：`work_queue` → `fetch_states` → `searchLogs` → `search_keywords`。
6. [ ] 部署云函数及依赖，配置环境变量和定时触发器。
7. [ ] 对比源/目标集合数量，抽查用户、普通帖、周边帖、留言、投票和消息关联 ID。
8. [ ] 按第 3 节权限矩阵设置权限，然后执行核心回归清单。
9. [ ] 记录恢复总耗时、失败文件、重试次数和最终差异；未完成一次空环境恢复前，不宣称备份可用。

## 6. 回滚条件

- 出现新的 `-502003`、列表空白、分享详情打不开、投票/评论/发布失败时，立即将当次集合权限恢复为变更前值。
- 索引创建失败不删除已有索引；保留控制台错误中给出的字段顺序作为下次依据。
- 导入不使用“覆盖已有集合”作为首次恢复方式；先在空环境验证，避免 `_id` 冲突和部分导入。

## 7. 当前结论

- [x] 完成代码级索引证据盘点。
- [x] 完成权限目标矩阵和不可立即收紧项识别。
- [x] 完成备份命名、完整性校验和空环境恢复顺序。
- [ ] 测试环境建索引并记录控制台结果。
- [ ] 测试环境设置权限并执行回归。
- [ ] 完成一次空环境导入和恢复演练。
