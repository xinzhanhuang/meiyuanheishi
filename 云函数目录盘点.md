# 云函数目录盘点

> 盘点日期：2026-08-11；本地仓库复核：2026-08-29
> 已完成本地静态盘点和云开发控制台核对，未删除任何目录。

## 结论

- 云函数一级目录：121 个。
- 完全空目录：99 个。
- 包含有效源码的目录：22 个。
- 前端实际调用（包括统一调用封装）：19 个。
- 包含本地触发器配置：3 个。
- 空目录中有 12 个名称明确属于 CMS、低代码或微信支付能力，暂不列入普通删除候选。
- 云开发控制台当前共有 118 个已部署云函数。
- 原定的 35 个普通空目录候选，在云端全部存在同名正常运行函数。
- 因此本轮可安全删除的云函数目录为 **0 个**。

## A. 使用中：前端有明确 `callFunction` 调用

| 云函数 | 主要用途 | 静态证据 |
| --- | --- | --- |
| `VoteOption` | 投票 | `pages/plate2/plate2.js` |
| `checkImg` | 图片内容审核 | 发帖页、帖子详情页 |
| `checkStr` | 文本内容审核 | 发帖页、帖子详情页 |
| `checknotice` | 管理员审核通知 | `pages/checkuser/checkuser.js` |
| `delete` | 删除帖子/评论 | 普通帖和周边帖详情页 |
| `dianzan` | 帖子/评论点赞 | 首页、列表、详情页 |
| `fbpl` | 发布普通帖评论 | `pages/plate2/plate2.js` |
| `fbzbpj` | 发布周边评论 | `pages/plate-zhoubian/plate-zhoubian.js` |
| `getSearchSuggestions` | 搜索建议 | `pages/plate4/plate4.js` |
| `getTagPostCount` | 标签帖子数 | `pages/plate4/plate4.js` |
| `getphone` | 获取手机号 | `pages/my/set/set.js` |
| `getworkmessage` | 管理工作消息 | `pages/work_manager/work_manager.js`，同时含触发器配置 |
| `jubao` | 详情页举报 | 普通帖和周边帖详情页 |
| `jubaoplus` | 列表举报/管理 | 首页、搜索页、点评页 |
| `login` | 获取 OpenID | `app.js` 及多个页面 |
| `look` | 浏览量统计 | 首页、列表、详情页 |
| `ordernotice` | 派单通知 | `pages/plate2/plate2.js` |
| `update_post_status` | 审核状态更新 | `pages/audit-list/audit-list.js` |

处理建议：**保留**。

## B. 使用中：本地有触发器配置

| 云函数 | 证据 | 处理建议 |
| --- | --- | --- |
| `AiNews` | `config.json` 含 `triggers` | 保留，后续核对云端触发器 |
| `getnewlines` | `config.json` 含 `triggers` | 保留，后续核对云端触发器 |
| `getworkmessage` | `config.json` 含 `triggers`，前端也有调用 | 保留 |

## C. 有效源码但本地未找到前端调用

| 云函数 | 现状 | 处理建议 |
| --- | --- | --- |
| `aggregateSearchKeywords` | 有 `index.js` 和 `package.json`，无本地触发器配置 | 疑似手工/云端调用，暂不删除 |

## D. 平台/第三方空目录

- `wx-ext-cms-api`
- `wx-ext-cms-fx-openapi`
- `wx-ext-cms-init`
- `wx-ext-cms-openapi`
- `wx-ext-cms-service`
- `wx-ext-cms-sms`
- `wxpayFunctions`

这些目录当前虽然为空，但名称属于 CMS/微信支付能力。在确认小程序不使用对应扩展、支付或控制台部署前，**不删除**。

## E. 本地为空、但云端已部署的目录

以下 35 个目录在本地完全为空，但已通过环境 `tafaheishi-1gs4bxsvcf864035` 的云开发控制台逐项搜索，确认云端全部存在同名函数，状态为正常：

- `Entrust`
- `InitInfo`
- `PublishEntrust`
- `agot`
- `cloud`
- `delNote`
- `deleteOrder`
- `echo`
- `exportMarkdownToPdf`
- `getNow`
- `getPhoneNumber`
- `getQuestionNoteList`
- `getTime`
- `getwordslist`
- `goods`
- `index`
- `login1`
- `loginAndRegistration`
- `loginword`
- `quickstartFunctions`
- `reduceUserPoints`
- `removeRecords`
- `setUserInfo`
- `shop_pay`
- `shop_refund`
- `shop_update_sku`
- `tempUpdateStatus`
- `update`
- `updateDetail`
- `updateExamNum`
- `updateKeep`
- `updateOrder`
- `updateQCorrectCount`
- `updateQNoteLikesAndCollNum`
- `updateRecords`

处理建议：**全部保留**。本地空目录可能是微信开发者工具中用于关联、下载或管理已部署云函数的占位目录。在未确认这些云函数的实际调用方和本地管理方式前，不做删除。

## 云端核对记录

1. 环境 ID：`tafaheishi-1gs4bxsvcf864035`。
2. 控制台显示已部署云函数共 118 个。
3. 通过控制台云函数名搜索，对 35 个候选名称逐项执行精确同名比对。
4. 35 个候选均返回精确同名函数，界面显示状态为“正常”。
5. 本次只读取控制台状态，未触发删除、部署、编辑或触发器变更。

## 本步骤验证

- 使用 `find` 统计一级目录、空目录和非空目录。
- 使用 `rg` 扫描前端 `wx.cloud.callFunction` 调用。
- 检查所有云函数 `config.json` 中的 `triggers` 配置。
- 使用云开发控制台逐项核对 35 个本地空目录的同名已部署函数。
- 本步骤没有改变任何业务代码、云函数或用户数据。
