# 云函数目录盘点

> 盘点日期：2026-08-11
> 本步骤仅做静态盘点，未删除任何目录。

## 结论

- 云函数一级目录：63 个。
- 完全空目录：42 个。
- 包含文件的目录：21 个。
- 前端明确调用：17 个。
- 包含本地触发器配置：3 个。
- 空目录中有 7 个名称明确属于 CMS/微信支付能力，暂不列入普通删除候选。
- 其余 35 个空目录可作为本地删除候选，但删除前仍需确认云开发控制台不存在同名已部署函数、触发器或外部调用。

## A. 使用中：前端有明确 `callFunction` 调用

| 云函数 | 主要用途 | 静态证据 |
| --- | --- | --- |
| `VoteOption` | 投票 | `pages/plate2/plate2.js` |
| `checkImg` | 图片内容审核 | 发帖页、帖子详情页 |
| `checkStr` | 文本内容审核 | 发帖页、帖子详情页 |
| `delete` | 删除帖子/评论 | 普通帖和周边帖详情页 |
| `dianzan` | 帖子/评论点赞 | 首页、列表、详情页 |
| `fbpl` | 发布普通帖评论 | `pages/plate2/plate2.js` |
| `fbzbpj` | 发布周边评论 | `pages/plate-zhoubian/plate-zhoubian.js` |
| `getSearchSuggestions` | 搜索建议 | `pages/plate4/plate4.js` |
| `getTagPostCount` | 标签帖子数 | `pages/plate4/plate4.js` |
| `getphone` | 获取手机号 | `pages/my/set/set.js` |
| `getworkmessage` | 管理工作消息 | `pages/work_manager/work_manager.js`，同时含触发器配置 |
| `jubao` | 详情页举报 | 普通帖和周边帖详情页 |
| `jubaoplus` | 列表举报/管理 | 首页、搜索页、点评页 |
| `login` | 获取 OpenID | `app.js` 及多个页面 |
| `look` | 浏览量统计 | 首页、列表、详情页 |
| `ordernotice` | 派单通知 | `pages/plate2/plate2.js` |
| `update_post_status` | 审核状态更新 | `pages/audit-list/audit-list.js` |

处理建议：**保留**。

## B. 使用中：本地有触发器配置

| 云函数 | 证据 | 处理建议 |
| --- | --- | --- |
| `AiNews` | `config.json` 含 `triggers` | 保留，后续核对云端触发器 |
| `getnewlines` | `config.json` 含 `triggers` | 保留，后续核对云端触发器 |
| `getworkmessage` | `config.json` 含 `triggers`，前端也有调用 | 保留 |

## C. 有效源码但本地未找到前端调用

| 云函数 | 现状 | 处理建议 |
| --- | --- | --- |
| `aggregateSearchKeywords` | 有 `index.js` 和 `package.json`，无本地触发器配置 | 疑似手工/云端调用，暂不删除 |
| `checknotice` | 有 `index.js`、`package.json` 和 `config.json`，未找到前端调用 | 疑似历史功能，暂不删除 |

## D. 平台/第三方空目录

- `wx-ext-cms-api`
- `wx-ext-cms-fx-openapi`
- `wx-ext-cms-init`
- `wx-ext-cms-openapi`
- `wx-ext-cms-service`
- `wx-ext-cms-sms`
- `wxpayFunctions`

这些目录当前虽然为空，但名称属于 CMS/微信支付能力。在确认小程序不使用对应扩展、支付或控制台部署前，**不删除**。

## E. 普通空目录删除候选

以下 35 个目录在本地完全为空，且未发现可执行源码或本地触发器配置：

- `Entrust`
- `InitInfo`
- `PublishEntrust`
- `agot`
- `cloud`
- `delNote`
- `deleteOrder`
- `echo`
- `exportMarkdownToPdf`
- `getNow`
- `getPhoneNumber`
- `getQuestionNoteList`
- `getTime`
- `getwordslist`
- `goods`
- `index`
- `login1`
- `loginAndRegistration`
- `loginword`
- `quickstartFunctions`
- `reduceUserPoints`
- `removeRecords`
- `setUserInfo`
- `shop_pay`
- `shop_refund`
- `shop_update_sku`
- `tempUpdateStatus`
- `update`
- `updateDetail`
- `updateExamNum`
- `updateKeep`
- `updateOrder`
- `updateQCorrectCount`
- `updateQNoteLikesAndCollNum`
- `updateRecords`

注意：`cloud`、`echo`、`getTime`、`index`、`update` 等名称过于通用，普通全文搜索会出现大量误匹配；判定时已以准确的 `wx.cloud.callFunction` 函数名、本地文件和配置为主。

## 删除前必须完成

1. 在微信开发者工具/云开发控制台核对 35 个候选名称。
2. 确认不存在同名已部署函数、定时触发器、数据库触发器或 HTTP 调用。
3. 确认 CMS、微信支付和其他扩展能力的实际使用情况。
4. 删除时只处理已确认的目录，不动 21 个非空目录。
5. 删除后重新执行目录统计、函数名搜索、小程序编译和核心功能回归。

## 本步骤验证

- 使用 `find` 统计一级目录、空目录和非空目录。
- 使用 `rg` 扫描前端 `wx.cloud.callFunction` 调用。
- 检查所有云函数 `config.json` 中的 `triggers` 配置。
- 本步骤没有改变任何业务代码、云函数或用户数据。
