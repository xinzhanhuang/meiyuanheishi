# 项目结构与云端数据台账

> 盘点日期：2026-08-28。来源仅限当前仓库代码与本地目录；不是云开发数据库导出，不推断线上未出现在代码中的字段、索引、权限或云函数调用方。

## 1. 项目结构

| 目录/文件 | 职责 |
| --- | --- |
| `miniprogram/app.js` | 云环境初始化、全局用户状态、消息红点和实时监听 |
| `miniprogram/pages` | 20 个已在 `app.json` 注册的页面 |
| `miniprogram/utils/util.js` | 通用网络、评论定位、帖子参数兼容和登录返回 URL |
| `cloudfunctions` | 120 个云函数目录，其中 21 个含本地源码 |
| `scripts` | 不打包到小程序的静态验证脚本 |
| `项目优化修改标准.md` | 优化规则与完成记录 |

### 已注册页面（20 个）

| 页面组 | 页面 |
| --- | --- |
| 核心浏览 | `index`、`plate1`、`plate2`、`plate3`、`plate4`、`zuiretiezi`、`checkuser` |
| 发布与周边 | `post`、`post-zhoubian`、`plate-zhoubian` |
| 用户与消息 | `my/wd`、`my/set`、`my/about`、`message/xiaoxi`、`message/dianzan` |
| 管理与辅助 | `Bgd_control`、`work_manager`、`audit-list`、`bannerDetail`、`tools` |

## 2. 数据库集合与代码可见字段

字段是“代码观察到的常用字段”，不是完整 Schema；`ss_xx`、评论对象等嵌套结构应继续兼容旧数据。

| 集合 | 页面/云函数引用 | 已观察字段或结构 |
| --- | --- | --- |
| `users` | `app`、首页、我的、消息、详情、登录 | `_id`、`_openid`、`online`、`logintime`、`ban`、`allow`、`msgnb`、`wenzhang`、`message`、`dzmessage`、`pinglunguode`、`userinfo.{username,userphoto,gender,anonymous,zhuanye,isVIP,login,LCU}` |
| `ss` | 首页、板块、搜索、普通详情、发帖 | `_id`、`ss_xx`；常用嵌套字段含 `huifunr`、`dianzanid`、`voteOption`、作者/标题/图片/时间等历史字段 |
| `tianmeizhoubian` | 周边发布、周边详情、消息 | `_id`、`ss_xx`；与普通帖共用评论、点赞、图片等历史嵌套结构 |
| `tj` | 周边详情的 `liuyan=true` 分支 | 留言帖集合；详情页通过 `ku` 动态选择，结构兼容 `ss_xx.huifunr` |
| `VoteOption` | 普通详情、`VoteOption` 云函数 | `id`（帖子 ID）、投票选项及颜色/票数相关字段 |
| `VoteRecord` | 普通详情 | `voteItemId`、`voterId`、`colorIndex` |
| `lunbotu3` | 首页/详情 | 轮播图配置 |
| `system` | 首页、详情、管理 | `_id`、`glid` 及系统配置 |
| `work_queue` | 工作管理、`getworkmessage` | 管理工作队列字段（以云函数和页面为准） |
| `searchLogs` | 搜索页/搜索云函数 | 搜索关键词日志 |

## 3. 页面跳转统一字段

| 字段 | 当前兼容来源 | 用途 |
| --- | --- | --- |
| `postId` | 新字段，缺失时读旧 `id` | 帖子主 ID |
| `postType` | `ss`、`zhoubian`、`tj` | 选择详情页/集合 |
| `commentId` | 新旧均已有 | 定位主评论或回复并短暂高亮 |
| `replyId` | 新字段，暂未强制使用 | 回复级定位预留 |
| `source` | `message/share/...` | 入口来源 |
| `schoolId` | 多院校阶段新增 | 学校数据隔离边界 |

详细入口和旧全局状态见 `页面跳转与字段链路盘点.md`。

## 4. 云函数二次分类

### 统计与结论

- 一级目录：120 个；带 `index.js` 源码：21 个；本地空目录：99 个。
- 前端明确 `wx.cloud.callFunction`：17 个。
- 本地触发器配置：`AiNews`、`getnewlines`、`getworkmessage`。
- **可安全删除：0 个。** 本地为空只说明仓库未保存源码，不说明云端函数未部署、未被触发器/HTTP/控制台调用。

### A. 前端明确调用：保留

`VoteOption`、`checkImg`、`checkStr`、`delete`、`dianzan`、`fbpl`、`fbzbpj`、`getSearchSuggestions`、`getTagPostCount`、`getphone`、`getworkmessage`、`jubao`、`jubaoplus`、`login`、`look`、`ordernotice`、`update_post_status`。

### B. 有本地源码但未找到前端直接调用：保留并在云端核对

| 云函数 | 本地证据 | 处理 |
| --- | --- | --- |
| `AiNews` | 源码和触发器配置 | 保留，核对触发器 |
| `getnewlines` | 源码和触发器配置 | 保留，核对触发器 |
| `aggregateSearchKeywords` | 源码 | 可能供后台/定时任务使用，保留 |
| `checknotice` | 源码和配置 | 可能为历史通知流程，保留 |

### C. 本地空目录：按功能域分组，全部暂不删除

| 类别 | 目录 |
| --- | --- |
| 平台扩展/支付 | `wx-ext-cms-api`、`wx-ext-cms-fx-openapi`、`wx-ext-cms-init`、`wx-ext-cms-openapi`、`wx-ext-cms-service`、`wx-ext-cms-sms`、`wxpayFunctions`、`quickstartFunctions`、`lowcode-automation`、`lowcode-automation-preview`、`lowcode-datasource`、`lowcode-datasource-preview` |
| 题库/学习/模型 | `fetchExam`、`fetchKnowledge`、`fetchPlanQues`、`fetchQues`、`fetchRecords`、`getCards`、`getExamPointList`、`getQuestionNoteList`、`getUserCollectionContentList`、`getUserExamPointWrongQList`、`getUserReviewPageData`、`getUserWrongQList`、`getUserWrongQPointList`、`getUserWrongQPointList2`、`getUserWrongQuestionList`、`model_auto_sign_trigger`、`model_book_changeBook`、`model_userWords_clear`、`model_userWords_get`、`model_user_getInfo`、`recalcStats`、`syncCardProgress`、`updateExamNum`、`updateQCorrectCount`、`updateQNoteLikesAndCollNum` |
| 订单/商品/派单 | `Entrust`、`PublishEntrust`、`addOrder`、`cancelOrder`、`deleteOrder`、`goods`、`order`、`shop_pay`、`shop_refund`、`shop_update_sku`、`updateOrder`、`tempUpdateStatus`、`updateDetail` |
| 用户/数据维护 | `AdminManage`、`Manager`、`InitInfo`、`addUserActiveInfo`、`addUserPoints`、`getPhoneNumber`、`getUserInfo`、`init-database`、`login1`、`login11`、`loginAndRegistration`、`loginword`、`reduceUserPoints`、`removeRecords`、`setUserInfo`、`update`、`updateKeep`、`updateRecords` |
| 工具/同步/未知来源 | `Atimer`、`aaxios`、`add`、`addLookHistory`、`agot`、`ai-generate`、`autoCleanExportFiles`、`batchImport`、`callback`、`cloud`、`data-sync`、`delNote`、`echo`、`exportMarkdownToPdf`、`getExportPageData`、`getNow`、`getPackageQRCode`、`getSharedPackage`、`getTime`、`getdata`、`getdb`、`getuserwords`、`getwordslist`、`index`、`market-data`、`message`、`myCloudFunctions`、`openapi`、`openrouter`、`sync`、`user-library` |

### 删除前必须满足的条件

1. 云开发测试环境与正式环境均确认未部署或可以删除。
2. 无定时触发器、数据库触发器、HTTP 调用、控制台手工任务或其他小程序调用。
3. 不是 CMS、支付、低代码或其他扩展依赖。
4. 给出函数名、环境、调用证据和回滚包，经确认后再删除；不得直接清理目录。

## 5. 验证方式和边界

- `scripts/check-project-inventory.js` 检查目录数量、源码函数数量、关键台账条目和“不可删除”结论。
- `app.json`、页面中的 `collection()`、`callFunction()` 与 `cloudfunctions/*` 是本台账的静态依据。
- 线上实际数据字段、索引、权限、云端部署版本和触发器状态，必须在云开发控制台或导出数据中再次核对。
