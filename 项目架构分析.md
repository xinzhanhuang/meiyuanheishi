# 天美黑市小程序项目架构分析

## 项目概述

这是一个基于微信云开发的校园社区小程序,主要功能包括帖子发布、板块浏览、评论互动、点赞收藏、消息通知等社交功能。

## 技术栈

- **框架**: 微信小程序原生框架
- **云服务**: 微信云开发
  - 云数据库
  - 云函数
  - 云存储
- **云环境**: `tafaheishi-1gs4bxsvcf864035`

---

## 核心架构

### 1. 应用配置

#### [`app.json`](file:///Users/xiyunhui/Desktop/%20天美黑市%202025.5.8/xiaoyuan-xiaochengxu-heishi/miniprogram/app.json)
- **页面路由**: 19个页面
- **TabBar**: 2个标签页(首页、我的)
- **权限**: 位置信息权限
- **扩展库**: WeUI组件库

#### [`app.js`](file:///Users/xiyunhui/Desktop/%20天美黑市%202025.5.8/xiaoyuan-xiaochengxu-heishi/miniprogram/app.js)
- 云开发初始化
- 全局用户信息管理 (`userInfo`)
- 在线状态管理
- 版本更新检测
- 全局变量:
  - `glids`: 管理员ID列表
  - `message`: 消息列表
  - `hongdian`: 红点标记
  - `shuaxin`: 刷新标记

---

## 页面模块

### 首页模块 (`pages/index`)

**核心功能**:
- 轮播图展示
- 热门搜索关键词
- 在线人数统计
- 帖子列表(最新/最热切换)
- 板块导航
- 点赞、收藏、预览图片
- 实时消息监听

**关键方法**:
- `getBannerList()`: 获取轮播图
- `hotsearckeys()`: 热门搜索
- `currentOnlineNum()`: 在线人数
- `jiazai()`: 加载帖子数据
- `dianzan()`: 点赞功能
- `jianting()`: 消息监听

### 板块模块 (`pages/plate1-4`, `pages/plate-zhoubian`)

**功能**:
- 板块内帖子浏览
- 搜索功能
- 最新/最热排序
- 点赞、评论
- 详情跳转

**数据表**:
- `plate1-4`: 使用 `ss` 数据表
- `plate-zhoubian`: 使用 `tianmeizhoubian` 数据表

### 发帖模块 (`pages/post`, `pages/post-zhoubian`)

**核心流程**:
1. 图片上传(最多9张)
2. 图片压缩处理
3. 内容审核(文本+图片)
4. 数据提交到云数据库
5. 更新用户发帖记录

**审核机制**:
- `checkStr()`: 文本内容审核
- `checkImg()`: 图片内容审核
- `yasuo()`: 图片压缩(Canvas)

**特殊功能**:
- 投票功能
- 派单功能(周边)
- 匿名发帖
- 位置标记

### 个人中心 (`pages/my/wd`)

**功能**:
- 用户登录/注册
- 个人信息展示
- 我的帖子
- 消息通知
- 设置入口

### 消息系统 (`pages/message/xiaoxi`)

**消息类型**:
- 评论消息
- 回复消息
- 点赞消息
- 系统消息

### 其他功能页面

- `pages/Bgd_control`: 后台管理控制
- `pages/checkuser`: 用户审核
- `pages/bannerDetail`: 轮播图详情
- `pages/zuiretiezi`: 最热帖子

---

## 云函数架构

### 认证相关

#### `login`
- 获取用户OpenID
- 返回微信上下文信息

### 内容审核

#### `checkStr`
- 文本内容合规检测

#### `checkImg`
- 图片内容合规检测

### 互动功能

#### `dianzan`
- 帖子点赞/取消点赞
- 评论点赞/取消点赞
- 支持类型:
  - `ss`: 普通帖子
  - `tianmeizhoubian`: 周边帖子
  - `sspinglun`: 评论点赞
  - `sszbpinglun`: 周边评论点赞

#### `fbpl` (发布评论)
- 评论帖子
- 回复评论
- 消息推送
- 订阅通知发送

#### `fbzbpj` (发布周边评价)
- 周边帖子评论功能

### 其他云函数

- `look`: 浏览量统计
- `delete`: 删除帖子/评论
- `jubao`: 举报功能
- `jubaoplus`: 增强举报
- `VoteOption`: 投票选项管理
- `AiNews`: AI新闻生成
- `getTagPostCount`: 获取标签帖子数量
- `getphone`: 获取手机号
- `ordernotice`: 订单通知
- `checknotice`: 检查通知

---

## 数据库结构

### 核心数据表

#### `users` (用户表)
```javascript
{
  _openid: String,
  _id: String,
  userinfo: {
    username: String,
    userphoto: String,
    gender: String,
    zhuanye: String,
    isVIP: Boolean,
    login: Boolean
  },
  wenzhang: Array,      // 发帖记录
  message: Array,       // 消息列表
  pinglunguode: Array,  // 评论过的记录
  ban: Boolean,         // 封禁状态
  online: Boolean,      // 在线状态
  msgnb: Array,         // 消息通知次数
  allow: Boolean        // 订阅授权
}
```

#### `ss` (帖子表)
```javascript
{
  _id: String,
  ss_xx: {
    choosetitle: String,      // 标签
    firsttime: Number,        // 发布时间
    username: String,         // 用户名
    gender: String,           // 性别
    userphoto: String,        // 头像
    nr: String,               // 内容
    tp: Array,                // 图片数组
    huifunr: Array,           // 评论数组
    huifunb: Number,          // 评论数
    dianzanid: Array,         // 点赞用户ID
    dianzannb: Number,        // 点赞数
    Mazhu: Array,             // 收藏用户ID
    jubao: Array,             // 举报记录
    look: Number,             // 浏览量
    lzid: String,             // 楼主ID
    orderdetail: Object       // 派单详情(可选)
  },
  voteNumberPerPerson: Number, // 投票限制
  voteOption: Array,           // 投票选项
  votepeopleNumber: Number,    // 投票人数
  isEnd: Boolean               // 投票结束
}
```

#### `tianmeizhoubian` (周边帖子表)
- 结构类似 `ss` 表
- 用于周边点评功能

#### `system` (系统配置表)
- 系统参数配置
- 轮播图配置
- 审核开关

#### `searchLogs` (搜索日志表)
- 搜索关键词统计

#### `VoteOption` (投票选项表)
- 投票选项详情
- 投票数统计

---

## 工具类与组件

### 工具类 (`utils/`)

#### `util.js`
- 时间格式化
- 通用工具函数

#### `filter.wxs`
- WXS过滤器
- 数据格式化

### 自定义组件 (`zujian/`)

#### `Tabs`
- 标签页切换组件
- 用于最新/最热切换

---

## 关键业务流程

### 1. 用户登录流程
```
1. 调用 login 云函数获取 OpenID
2. 查询 users 表检查用户是否存在
3. 不存在则创建新用户记录
4. 更新全局 userInfo
5. 设置在线状态
```

### 2. 发帖流程
```
1. 选择图片(可选,最多9张)
2. 填写内容
3. 文本内容审核(checkStr)
4. 图片压缩(yasuo)
5. 图片审核(checkImg)
6. 上传图片到云存储
7. 提交帖子数据到 ss 表
8. 更新用户 wenzhang 记录
9. 跳转到首页
```

### 3. 评论流程
```
1. 填写评论内容
2. 调用 fbpl 云函数
3. 更新帖子 huifunr 数组
4. 给帖子主/被回复者发送消息
5. 发送订阅通知(离线用户)
6. 更新评论者 pinglunguode 记录
```

### 4. 点赞流程
```
1. 调用 dianzan 云函数
2. 检查用户是否已点赞
3. 未点赞: 添加到 dianzanid, dianzannb +1
4. 已点赞: 从 dianzanid 移除, dianzannb -1
5. 更新本地数据
```

### 5. 消息监听流程
```
1. 监听 users 表的当前用户文档
2. 检测 message 字段变化
3. 更新 TabBar 红点
4. 刷新消息列表
```

---

## 特色功能

### 1. 实时消息推送
- 使用云数据库 `watch` API
- 监听用户文档变化
- 实时更新红点提示

### 2. 订阅消息
- 评论通知
- 回复通知
- 离线推送

### 3. 内容审核
- 文本审核
- 图片审核
- GIF动图审核

### 4. 图片处理
- Canvas压缩
- 多尺寸压缩
- 格式转换

### 5. 投票功能
- 单选/多选
- 实时统计
- 防重复投票

### 6. 派单功能
- 位置标记
- 赏金设置
- 接单管理

---

## 性能优化

1. **图片懒加载**: 使用 `imageOnLoad2` 预加载
2. **分页加载**: 帖子列表分页
3. **数据缓存**: 全局变量缓存用户信息
4. **图片压缩**: 减少云存储和流量消耗
5. **按需监听**: 仅在需要时启动消息监听

---

## 安全机制

1. **内容审核**: 文本和图片双重审核
2. **封禁系统**: `ban` 字段控制用户权限
3. **举报功能**: 用户举报机制
4. **管理员权限**: `glids` 管理员列表
5. **OpenID鉴权**: 云函数自动鉴权

---

## 项目特点

✅ **完整的社区功能**: 发帖、评论、点赞、收藏、消息
✅ **实时互动**: 消息监听、在线状态
✅ **内容安全**: 多重审核机制
✅ **用户体验**: 图片预览、搜索、排序
✅ **扩展功能**: 投票、派单、周边点评
✅ **云原生**: 充分利用微信云开发能力

---

## 待优化方向

1. 代码模块化和复用性
2. 错误处理和异常捕获
3. 性能监控和日志系统
4. 单元测试覆盖
5. 数据库索引优化
6. 图片CDN加速
