# 帖子模型、服务层与异步状态契约

> 目标：在不删除 `ss_xx`、不改变页面交互和旧数据读取的前提下，逐步建立可迁移、可测试的工程边界。

## 1. 帖子公共字段

新发布内容在集合文档顶层写入以下字段，旧页面继续读取原有 `ss_xx`：

| 字段 | 含义 | 旧数据兼容 |
| --- | --- | --- |
| `postType` | `post`、`order`、`activity`、`zhoubian` | 读取时由 `orderdetail` 等旧字段推导 |
| `schoolId` | 所属院校；当前允许为空 | 旧数据默认空，后续迁移时补默认院校 |
| `authorId` | 云端登录用户对应的 `users._id` | 回退到 `ss_xx.lzid` |
| `status` | `active` 或 `closed` | 回退到活动/派单状态 |
| `createdAt` | 创建时间戳 | 回退到 `time/firsttime` |
| `updatedAt` | 最后更新时间戳 | 回退到创建时间 |
| `requestId` | 发布请求幂等键 | 仅新发布链路写入 |

派单字段继续放在 `ss_xx.orderdetail`，活动状态继续兼容 `ss_xx.isover`，投票字段继续保留在文档顶层。此阶段不拆集合、不批量迁移旧数据。

## 2. 客户端数据访问层

| 服务 | 职责 |
| --- | --- |
| `services/post-service.js` | 帖子读取、浏览计数、举报、管理、点赞、码住、接单和发布 |
| `services/user-service.js` | 登录身份、用户读取、系统配置和当前用户动作 |
| `services/comment-service.js` | 评论发布、删除、点赞和文字审核 |
| `services/vote-service.js` | 投票状态读取和投票提交 |
| `services/home-service.js` | 首页配置、热词、轮播、置顶帖和分页列表查询 |

`pages/plate2/plate2.js` 只编排页面交互；数据库和云函数名称不再散落在主页面。图片审核暂留在 `plate2-images.js`，因为它包含文件缓冲区和画布流程，不强行塞进以上四个业务服务。

## 3. 云函数兼容返回

新契约：

```js
{
  success: true,
  code: 'OK',
  message: '',
  data: {},
  requestId: ''
}
```

迁移期间仍保留 `id/record/errCode/errMsg` 等旧顶层字段。`utils/cloud-call.js` 同时兼容新旧格式，并把微信云调用返回的 `requestID` 注入结果或错误对象。

## 4. 状态与重复提交

- 发布：`publishState = loading → success/failed`，页面锁阻止连续点击；同一 `requestId` 重试返回已有帖子。
- 评论：`commentSubmitState = loading → success/failed`，只有云端成功后才更新评论列表和清空草稿。
- 投票：`voteSubmitState = loading → success/failed`，页面锁阻止重复提交，云端事务继续校验个人票数。
- 点赞和码住：在途锁阻止重复点击，云端成功后才更新本地状态。
- 举报/管理员封帖：数据库事务同时更新帖子、用户违规记录与消息，失败不显示成功。

## 5. 部署与索引

- 需要重新部署：`publishPost`、`jubao`、`jubaoplus`。
- 发布幂等查询需要测试环境确认复合索引：`ss(authorId, requestId)`、`tianmeizhoubian(authorId, requestId)`。
- 新云函数先部署到测试环境；旧客户端仍可调用，因为旧字段和顶层返回均保留。

## 6. 后续边界

- [x] 普通详情核心数据调用进入四个 service。
- [x] 普通详情点赞、码住交互从主页面迁移到 `plate2-interactions.js`。
- [x] 新发布帖子写入兼容公共字段和请求幂等键。
- [x] 普通详情评论、点赞、码住等待云端结果。
- [x] 举报与管理员封帖改为事务。
- [x] 周边详情迁移至相同 service：页面内不再直接访问数据库或调用云函数，评论 API 拆入 `plate-zhoubian-comments.js`。
- [x] 首页与个人中心不再直连数据库或云函数；用户会话、首页查询、帖子写操作分别经由 app 会话层和对应 service。
- [ ] 其他写云函数按风险逐个迁移统一返回，不一次性更改全部线上协议。
- [ ] 测试环境创建幂等索引并完成真实设备回归。
